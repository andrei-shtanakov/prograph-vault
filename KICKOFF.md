# KICKOFF — implement Robin for the AI-Orchestrators ecosystem

Paste this into your coding agent.

---

Implement **Robin** according to `./ROBIN-SPEC.local.md` on this stack:

> **Telegram** (chat) · **headless `claude` CLI on cron** (agent runtime) · **always-on
> VPS** (hosting + scheduler) · **flat files** (storage) · read-only MCP bindings to
> **Linear, Gmail, Google Drive/Calendar** · knowledge repo **prograph-vault** mounted
> read-only.

(That sentence names platforms and service bindings only. Choose libraries, packages, and
project layout yourself — they are not fixed by the spec.)

Robin's identity is `./soul.md` and its duty roster is `./robin/duties.md` — **versioned
team files; they change by PR, never by config.** Read both before writing code.

**Where the code lives:** choose and document a home. The knowledge repo (prograph-vault)
stays **read-only at Robin's runtime** — a sibling directory is the usual answer (e.g.
`../robin-runtime/`), with prograph-vault and the other ecosystem repos cloned/mounted
read-only beside it. Do not put runtime code inside prograph-vault, and do not let the
operator's personal host config (personal MCP servers, global instructions, memory) leak
into Robin's workspace (§6.5).

**Build M0 first.** Its acceptance test:

> From a fresh agent session, **"Which repo owns the agents-catalog SSOT?"** is answered
> **`atp-platform/method/agents-catalog.toml`**, citing
> `authored/decisions/2026-07-01-adr-eco-003-agent-catalog.md` (amendment 2026-07-03) —
> read from prograph-vault, source file cited.

Then M1 (the same question asked by a teammate in the team Telegram channel, answered with
citation) — that is the demonstration event. Then M2–M4 per the §8 checklist in
`./ROBIN-SPEC.local.md`. M5 (meetings) is deferred — no capture connected.

**Do not skip the mandatory hardening** in the local spec's Appendix — read-back
verification (§6.4), negative-evidence + liveness + cost caps (§7), context isolation
(§6.5), and Telegram output escaping (§6.7). They are written from the reference's
failures and do not happen by default.

**Log every clarifying question you have to ask in `./QUESTIONS.md`** — each one is a spec
bug for upstreaming. Do not patch `ROBIN-SPEC.local.md` to answer them yourself; the
configurator's own open findings are already at the top of `./QUESTIONS.md` for you to
resolve.

---

*Generated by /robin-init on 2026-07-07. Upstream spec: ROBIN-SPEC.md @ git b5ad49e
(sha256 f8f49c73…).*
