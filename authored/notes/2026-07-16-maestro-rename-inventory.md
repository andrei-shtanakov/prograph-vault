---
title: Maestro lowercase rename inventory
type: note
status: proposed
owner: Andrei
updated: 2026-07-16
---

# TL;DR

- `Maestro -> maestro` is mostly a repo/path lowercase migration; package and CLI are already `maestro`.
- Exact path/remote hits are concentrated in `_cowork_output`, `prograph-vault`, `Maestro`, and neighbor `CLAUDE.md` files.
- Active replacement targets: `Maestro/` paths, `../Maestro/`, `andrei-shtanakov/Maestro(.git)`, workspace manifest key/dir.
- Do not mass-lowercase product prose such as "Maestro orchestrates..." or UI titles like "Maestro Dashboard".

# Maestro lowercase rename inventory

This note mirrors `_cowork_output/integration/2026-07-16-maestro-rename-inventory.md`.

## Current Core Facts

| Item | Value |
|---|---|
| Local directory | canonical `maestro/`; on current macOS checkout `Maestro` and `maestro` resolve to the same inode |
| Current branch | `master` |
| HEAD | `ab3b4a3 chore: add repository code owner` |
| Remote | `git@github.com:andrei-shtanakov/maestro.git` |
| Python package | `maestro` |
| CLI command | `maestro` |
| Version | `0.4.0` |

## Summary Counts

| Pattern family | Main hits |
|---|---|
| Exact path/remote patterns | `_cowork_output` 71, `prograph-vault` 70, `Maestro` 14, `arbiter` 12, `spec-runner` 8 |
| Broad product name `Maestro` | `Maestro` 149, `_cowork_output` 120, `prograph-vault` 115, `arbiter` 35, `spec-runner` 33 |

## KB-Relevant Update Targets

| Target | Action |
|---|---|
| `authored/registry/registry.md` | active project row updated from `Maestro` path identity to `maestro` after repo rename |
| `authored/notes/2026-07-08-migration-runbook.md` | clone/push list and GitHub URL updated after remote rename |
| living ecosystem maps | `Maestro/` paths updated to `maestro/` where the map is current |
| `authored/notes/status/*` | preserve historical dated paths |
| `derived/**` | do not manually edit unless regenerated by the owning tool |

## Boundary Rules

- The KB records the cross-cutting decision and active registry view.
- Product brand remains `Maestro`; path identity becomes `maestro`.
- Runtime code must not read `_cowork_output`.
- Historical reports keep dated facts.

## Next Handoffs

| Repo | Handoff |
|---|---|
| `maestro` | core repo-local docs and remote/local rename complete |
| `ai-orchestrators-workspace` | manifest key/dir/remote update complete |
| `devtools` | path-dependent scripts and VS Code workspace updated |
| `dispatcher` | watched path config from `Maestro/...` to `maestro/...` updated |
| Neighbor repos | `CLAUDE.md` sibling lists from `../Maestro/` to `../maestro/` updated |
