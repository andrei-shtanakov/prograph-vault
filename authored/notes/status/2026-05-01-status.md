---
title: Weekly Status Check — 2026-05-01
type: note
status: archived
owner: Andrei
updated: 2026-05-01
archived: 2026-07-06
reason: historical status snapshot (bulk-archived on graduation from _cowork_output)
---

# Weekly Status Check — 2026-05-01

> Period: 2026-04-24 (previous report) → 2026-05-01 (7 days)
> Scope: Maestro, arbiter, atp-platform, spec-runner, proctor-a, open-prose, agents-for-game
> Mode: READ-ONLY (automated scheduled-run)
> Source: `git log --since="2026-04-24"`, grep TODO/FIXME, cross-checked with `_cowork_output/status/2026-04-24-status.md`, `COWORK_CONTEXT.md`, Maestro/arbiter `TODO.md`, `Maestro/CHANGELOG.md`.

## TL;DR

1. **The main event of the week — `arbiter#9` closed by a two-sided PR (2026-04-25)**. Arbiter `d1a8ecd` added `decision_id` (i64 SQLite rowid) to the `route_task` response.metadata + Rust observability v1 (`arbiter-core::obs`, OTel Logs JSONL per the shared schema). Maestro's paired commit `e5915f2` coerces `int → str`. **This unblocked R-05** — Maestro `f1f7d26` wrote real subprocess tests against the `arbiter-mcp` binary (skip-by-default, `MAESTRO_ARBITER_BIN`).
2. **Maestro closed observability M2** (`d474120`, 2026-04-25). The scheduler is instrumented with OTel spans (`scheduler.session`, `task.spawn`), all three spawners (`aider`/`claude_code`/`codex`) propagate `TRACEPARENT` via `spawn_env()`. The previous caveat "live JSONL = 0 bytes" is no longer relevant.
3. **atp-platform — 31 commits**, movement across 3 themes: (a) **MCP plan Tasks 1/3/4** — a cache for API-token resolution (`dea05c1`), a warmup `ping` tool (`c10ceed`), structured handshake observability (`b7f7138`); (b) **El Farol**: wire-format migration `slots → intervals` (#98 `1e9d2859`), drop of the legacy flat-slot format (#105 `6d480b27`), a live SSE dashboard (`818f127` + `7d7c179`); (c) logging/deploy: `alembic upgrade head` at container start, fix MCP session_id, drop log propagation in prod.
4. **spec-runner — 0 commits** for 12 days (last `fa6b106` 2026-04-19). After the `logging→obs` cutover the team didn't return — the R-04 freeze continues.
5. **proctor-a — 0 commits** for 15 days. The pause since 2026-04-16 continues, a strong contrast with the previous sprint.
6. **open-prose — 0 commits** for 24 days. Spec-only status confirmed.
7. **agents-for-game (still NOT a git repo, NOT in the registry)**: 5 new files since 2026-04-24, including `claude_el_farol_3bots.py` (30KB) and `deepseek_el_farol_3bots.py` (33KB) + 9 smoke logs. A proving ground for LABS-TSA, a live local link to ATP's MCP/auth changes.
8. **TODO/FIXME — general downward trend**: Maestro 5 (+2 from 3, new from R-05/M2), arbiter 3 (+3 from 0, new from obs.rs), atp-platform 1 (-3 from 4 — cleanup), spec-runner 16 (unchanged), proctor-a 1, open-prose 0.

---

## 1. Per-project snapshot

### Maestro
- **Last commit:** `b9a5804 docs fixing` (2026-04-26)
- **Version (pyproject):** 0.2.0 (no bump)
- **Commits since 2026-04-24:** 5
- **Files changed:** 12 (including 4 spawners + scheduler + 2 new test modules)
- **TODO/FIXME/HACK (excluding tests):** 5 (was 3)
- **Key themes:**
  - **arbiter#9 client-side fix** — `e5915f2 fix(arbiter#9): coerce decision_id int→str in routing extractor`. The `decision_id TEXT` contract (Maestro SQLite) is aligned with `i64` (arbiter Rust).
  - **R-05 e2e — real subprocess tests** — `f1f7d26 test(arbiter): R-05 real-subprocess e2e tests against arbiter-mcp binary` → a new file `tests/test_arbiter_real_subprocess.py` (198 lines, 4 tests), skip-by-default via binary detection, `MAESTRO_ARBITER_BIN` override, sibling-repo layout autodetect.
  - **observability M2** — `d474120 feat(obs): M2 — instrument scheduler + propagate trace through spawners`. A top-level `scheduler.session` span, a `task.spawn` span on the ASSIGN path, four `_obs_log` lifecycle events (`task.completed`/`validation_failed`/`failed`/`timeout`) with `retry_count`/`will_retry`/`agent`. A new helper `spawn_env()` merges `os.environ` + `child_env()` → all three spawners receive `TRACEPARENT` + `ORCHESTRA_PIPELINE_ID`.
  - **TODO snapshot** — `1f541eb docs(todo): snapshot 2026-04-25 — R-05 contract-level, arbiter#9, M1+M2 obs`.
- **Contract pin (vendored)**: `maestro/_vendor/obs.py` is still at `spec-runner@fa6b106` — unchanged (there were no upstream commits).
- **Blockers:** none; the external P0 (`arbiter#9`) is **closed**.

### arbiter
- **Last commit:** `3d140b2 docs fixing` (2026-04-26)
- **Version:** 0.1.0 (workspace, no bump)
- **Commits since 2026-04-24:** 3
- **Files changed:** 27 (including Rust core + MCP server + Python orchestrator)
- **TODO/FIXME/HACK (excluding tests):** 3 (was 0; new — inside `arbiter-core/src/obs.rs`)
- **Key themes:**
  - **observability v1 + arbiter#9** (one commit, two threads) — `d1a8ecd feat(obs+#9): observability v1 (Rust + vendored Python) + surface decision_id`. Touched: `arbiter-core/src/obs.rs` (new), `arbiter-core/tests/emit_contract.rs` + `fixtures_contract.rs` (validation against the shared `log-schema.json`), `arbiter-mcp/src/tools/route_task.rs|report_outcome.rs|agent_status.rs`, `arbiter-mcp/src/server.rs|main.rs|agents.rs`. The Python `orchestrator/_vendor/obs.py` is vendored from the same `spec-runner@fa6b106`. `RouteResult.decision_id: Option<i64>`, `result_to_json` writes `metadata.decision_id`. **301 Rust tests green, clippy clean.**
  - **TODO snapshot** — `dc8368a docs: snapshot 2026-04-25 — arbiter#9 closed, obs v1 shipped, R-06b reframing pointer`.
- **`arbiter/TODO.md`:** R-10 ✅ closed, R-13 ✅ closed via analysis. **R-05 contract-level** unblocked, the real implementation is on the Maestro side (`f1f7d26`).
- **Blockers:** none.

### atp-platform
- **Last commit:** `e865b6f Ba (#114)` (2026-04-30)
- **Commits since 2026-04-24:** 31 (previous window: 54 — a normal decline)
- **Files changed:** 137 (excluding binary .png in `static/images/`)
- **TODO/FIXME/HACK (excluding tests, only `*.py`):** 1 (was 4 — **−3**, cleanup)
- **Key themes (by PR groups):**
  - **MCP plan Tasks 1/3/4 — observability and handshake stability:**
    - `dea05c1 feat(auth): in-process LRU cache for API-token resolution (Task 1) (#99)`
    - `c10ceed feat(mcp): add ping warmup tool (Task 3) (#100)`
    - `b7f7138 feat(mcp): structured handshake observability (Task 4) (#101)` — four events (`mcp_handshake_started`/`authorized`/`rejected`/`mcp_first_tool_call`), a TTLCache of 1h on `session_id`, a new module `packages/atp-dashboard/atp/dashboard/mcp/observability.py`, a runbook `docs/runbooks/mcp-observability.md`. `mcp_tools_list_responded` deferred — FastMCP has no public hook.
    - `01bd00a fix(mcp): emit_tool_call reads session_id from messages-POST URL (#103)`
    - `f985d56 spike: add /mcp-http streamable-HTTP mount + minimal MCP repro script (#106)`
  - **El Farol — wire-format breaking change** (a contract event, see §2):
    - `1e9d2859 Wire El Farol tournament telemetry end-to-end (#98)` — `ElFarolAction.intervals: list[list[int]]` replaces `slots`; +9 nullable columns on `tournament_actions` (retry/validation_error/decide_ms + agent self-reported model/tokens/cost + W3C trace_id/span_id), `ActionTelemetry` pydantic `extra="forbid"`, migration `a1b2c3d4e5f6_action_tier2_fields`.
    - `6d480b27 El Farol: drop legacy flat-slot move format (#105)` — final removal of the legacy format.
    - `818f127 feat(dashboard): live El Farol tournament dashboard via SSE (#108)`, `7d7c179 feat(dashboard): live El Farol UI polish + per-round timer (#109)`.
  - **Logging and deploy:**
    - `b14c0f5 feat(logging): surface app INFO + structured extras to stdout (#102)`
    - `704b465 fix(logging): kill propagation in prod + one-shot startup dump (#104)`
    - `4276360 feat(deploy): alembic upgrade head on container start + prod reconcile (#86)` + `00a04b1 fix(deploy): COPY scripts/ in Dockerfile`.
  - **Admin UI and housekeeping:**
    - `0017688 feat(admin-ui): users page, sidebar link, create-tournament CTA, list cancel (#89)`, `ae99645 feat(ui): surface tournament ID on detail/list/post-create pages (#113)`.
    - `3bc3de7 docs(integration): close R-06a + R-13 (Maestro CLI contract + arbiter guardrails mapping) (#92)` — closure of two integration axes in `docs/maestro-integration.md`.
- **Blockers:** none.

### spec-runner
- **Last commit:** `fa6b106 fix(obs): use TRACEPARENT parent span_id as initial _span_id` (2026-04-19) — **unchanged for 12 days**.
- **Version:** 2.0.0 (no bump; R-04 freeze in effect).
- **Commits since 2026-04-24:** 0.
- **Files changed:** 0.
- **TODO/FIXME/HACK (excluding tests):** 16 (unchanged; markers in `task.py`/`cli.py`/`cli_plan.py`/`mcp_server.py`).
- **Contract pin:** Maestro and arbiter both vendor `obs.py` at `spec-runner@fa6b106` — in sync.
- **Blockers:** none.

### proctor-a
- **Last commit:** `53e8844 fix(tests): remove unused asyncio import in test_bus.py` (2026-04-16) — **unchanged for 15 days**.
- **Commits since 2026-04-24:** 0.
- **TODO/FIXME/HACK (excluding tests):** 1 (was 0; probably an artifact of a different counter scale).
- **Blockers:** none (pause since 2026-04-16, the registry status is correct — `🔨 Phase 2 (paused)`).

### open-prose
- **Last commit:** `6284194 plagin test` (2026-04-07) — **unchanged for 24 days**.
- **Registry status:** 📄 Spec Only — matches.
- **Blockers:** not applicable.

### agents-for-game (still NOT in the registry, NOT a git repo)
- **New files over the week (mtime > 2026-04-24):**
  - `claude_el_farol_3bots.py` (30743 B, 2026-04-28) — an extension of the previous `claude_el_farol_agents.py` to 3 bots.
  - `deepseek_el_farol_3bots.py` (33614 B, 2026-04-28) — a DeepSeek variant on the same primitives.
  - `play_el_farol_3bots.py` (19205 B, 2026-04-28) — a runner.
  - `bot1_player.py` (10187 B, 2026-04-24), `bot2_player.py` (8498 B, 2026-04-24).
  - 9 smoke logs (`smoke-r1-{1..5}.log`, `smoke-stagger15.log`, `smoke-streamable-{1,2}.log`, `smoke-retry1*.log`).
- **Context:** actively exploited as a proving ground for LABS-TSA → ATP MCP. The smoke logs correlate with `f985d56` (`/mcp-http` spike) and `01bd00a` (`session_id` fix) on the ATP side.
- **Risk (repeated from last week, escalating):** `.env` with real tokens, without `.gitignore`, without version control. This week `.env.example.el_parol` and `.env` were updated. It should be either in the ATP repo's `games/`, or explicitly `.gitignore`-d as a sandbox.

---

## 2. Contract points — summary

| Contract file / group | Changed this week? | Last change | Consumers | Synchronized? |
|---|---|---|---|---|
| `arbiter/arbiter-core/src/types.rs` | ❌ | no commits | arbiter-mcp, Maestro (vendored `861534e`) | ✅ frozen |
| `arbiter/arbiter-core/src/lib.rs` (`RouteResult`) | ✅ | `d1a8ecd` 2026-04-25 | arbiter-mcp/tools, Maestro routing | ✅ **both sides**: arbiter added `decision_id: Option<i64>`, Maestro `e5915f2` coerces `int→str` |
| `arbiter/arbiter-mcp/src/tools/route_task.rs` | ✅ | `d1a8ecd` 2026-04-25 | Maestro `arbiter_client.py` | ✅ `result_to_json` now writes `metadata.decision_id` |
| `arbiter/arbiter-mcp/src/tools/report_outcome.rs` | ✅ | `d1a8ecd` 2026-04-25 | Maestro `coordination/routing.py` | ✅ structured tracing labels added |
| `arbiter/config/agents.toml` | ❌ | `b18720d` 2026-02-07 | arbiter runtime | ✅ |
| `Maestro/maestro/coordination/routing.py` | ✅ | `e5915f2` 2026-04-25 | Maestro scheduler | ✅ extract_decision_id updated |
| `Maestro/maestro/spawners/{aider,claude_code,codex,base}.py` | ✅ | `d474120` 2026-04-25 | external CLI agents | ✅ TRACEPARENT via `spawn_env()` |
| `Maestro/maestro/scheduler.py` | ✅ | `d474120` 2026-04-25 | pipeline | ✅ wrapped in `scheduler.session` + `task.spawn` spans |
| `Maestro/maestro/_vendor/obs.py` | ❌ | `4688633` 2026-04-19 | all of Maestro | ✅ pinned at `spec-runner@fa6b106`, in sync with arbiter |
| `arbiter/orchestrator/_vendor/obs.py` | ✅ (new) | `d1a8ecd` 2026-04-25 | arbiter Python orchestrator | ✅ same pin `spec-runner@fa6b106` |
| `Maestro/_cowork_output/observability-contract/log-schema.json` | ❌ | `be29b16` 2026-04-19 | spec-runner test, Maestro vendored, arbiter `emit_contract.rs` | ✅ **the three-sided axis works**: spec-runner reference impl, Maestro vendored, arbiter Rust validates against the same schema |
| `spec-runner/schemas/` (executor-state, json-result) | ❌ | R-04 freeze 2026-04-17 | Maestro Pydantic | ✅ frozen |
| `atp-platform/.../mcp/auth.py` + `mcp/observability.py` (new) | ✅ | `b7f7138` / `dea05c1` / `01bd00a` | external ATP MCP clients (including `agents-for-game`) | ⚠️ see below |
| `atp-platform/.../tournament/schemas.py` (`ElFarolAction`) | ✅ **breaking** | `1e9d2859` 2026-04-27 + `6d480b27` 2026-04-28 | external ATP agents playing El Farol | ⚠️ wire-format `slots → intervals` |
| `atp-platform/packages/atp-core/atp/protocol/models.py` | ❌ | no commits since 2026-03-28 | external SDK | ✅ frozen |

### Wiring check (grep + git log)

- **`arbiter#9` (P0 from the last report) — closed by a two-sided commit 2026-04-25.** `arbiter/d1a8ecd` (`RouteResult.decision_id` + `result_to_json` + 301 Rust tests) ↔ `Maestro/e5915f2` (`int → str` coercion in `_extract_decision_id`). Proof-test — `tests/test_arbiter_real_subprocess.py` (Maestro `f1f7d26`), 4 subprocess tests against the real binary.
- **observability axis — all 4 consumers aligned**: spec-runner `obs.py@fa6b106` (reference) ↔ Maestro `_vendor/obs.py@fa6b106` ↔ arbiter `orchestrator/_vendor/obs.py@fa6b106` ↔ arbiter Rust `arbiter-core/src/obs.rs` (validated against the shared `log-schema.json` in `emit_contract.rs`).
- **ATP El Farol wire-format breaking change** (`1e9d2859` + `6d480b27`): `slots: list[int]` → `intervals: list[list[int]]` (`[start, end]` pairs, up to 2). Consumers: `agents-for-game/claude_el_farol_3bots.py` + `deepseek_el_farol_3bots.py` (new files 04-28 — probably already on the new format, but without a repo/tests it's impossible to confirm). **Recommendation**: look at the smoke logs `smoke-r1-*.log` from 04-28, whether there are 422/validation errors there.
- **ATP MCP auth observability** (`b7f7138`): the new `mcp_handshake_*` events ↔ for `agents-for-game` this is a useful signal for debugging race conditions in `claude_el_farol_3bots.py`/`deepseek_el_farol_3bots.py`. No breaking change.
- **Maestro coordination/spawners — 4 surfaces changed at once** (`e5915f2` + `d474120`): scheduler, validator, all 3 spawners. No external consumers, all inside Maestro.

---

## 3. Brief status (5–7 lines)

- **Progressed**: arbiter#9 P0 closed on both sides (arbiter `d1a8ecd` + Maestro `e5915f2`); R-05 unblocked → Maestro wrote real subprocess e2e tests (`f1f7d26`); observability M2 closed (Maestro scheduler+spawners instrumented, TRACEPARENT now flows live, not only in an integration test); arbiter received Rust observability v1 (a third party to the shared log-schema.json axis).
- **Blocked**: nothing critical. R-06b (Maestro ↔ ATP SDK adapter) is still `pending` — ATP is ready, the Maestro side is not started. R-07 waits on R-06b.
- **Needs attention**:
  1. **`agents-for-game/`** — the fifth consecutive week's request to either put it in the repo or in `.gitignore`. This week 2 large files (~30KB each) and 9 smoke logs appeared; `.env` with tokens was updated. Accumulation without VCS.
  2. **El Farol wire-format breaking** (`slots → intervals`) — external ATP MCP clients had to update. In our ecosystem this concerns only `agents-for-game/claude_el_farol_3bots.py`. Worth confirming the absence of 422 errors.
  3. **R-06b SDK adapter** — the only integration axis standing still for three weeks. ATP handed over the contract, the Maestro side is not planned in the current sprint.
  4. **proctor-a and open-prose** — 15 and 24 days without commits respectively. Consistent with the `🔨 paused` and `📄 Spec Only` statuses, but if the pause drags on another month — worth explicitly archiving in the registry.

---

## Recommended actions

| Action | Project | Priority | Rationale |
|---|---|---|---|
| Check the smoke logs `smoke-r1-*.log` (04-28) for 422/validation after `slots→intervals` | agents-for-game | P2 | Confirm the ATP breaking change `1e9d2859` is handled |
| Decide the fate of `agents-for-game/`: move into `atp-platform/games/` or `.gitignore` | whole cowork | P2 | 5th consecutive week; ~80KB of code + tokens without VCS have accumulated |
| Plan the R-06b SDK adapter (Maestro side) or explicitly defer it in the roadmap | Maestro / ATP | P3 | The only integration axis without progress; all others are either frozen or moving |
| Update `COWORK_CONTEXT.md`: mark arbiter#9 as closed, M2 as closed, add arbiter `obs.rs` to the observability map | _cowork_output | P3 | The current file from 2026-04-25 is outdated on the specific commits `d1a8ecd`/`d474120`/`e5915f2`/`f1f7d26` |
| Update proctor-a status in the registry if the pause > 30 days (currently 15) | _cowork_output | P3 | If no commits appear by 2026-05-16 — reclassify as `archived` |

---

## Appendix: data

### Commits since 2026-04-24

**Maestro (5):**
```
b9a5804 docs fixing (2026-04-26)
1f541eb docs(todo): snapshot 2026-04-25 — R-05 contract-level, arbiter#9, M1+M2 obs (2026-04-25)
f1f7d26 test(arbiter): R-05 real-subprocess e2e tests against arbiter-mcp binary (2026-04-25)
d474120 feat(obs): M2 — instrument scheduler + propagate trace through spawners (2026-04-25)
e5915f2 fix(arbiter#9): coerce decision_id int→str in routing extractor (2026-04-25)
```

**arbiter (3):**
```
3d140b2 docs fixing (2026-04-26)
dc8368a docs: snapshot 2026-04-25 — arbiter#9 closed, obs v1 shipped, R-06b reframing pointer (2026-04-25)
d1a8ecd feat(obs+#9): observability v1 (Rust + vendored Python) + surface decision_id (2026-04-25)
```

**atp-platform (31):** see the full list in the earlier git log fragment above — grouped in §1.

**spec-runner / proctor-a / open-prose:** 0 commits.

### Summary statistics

| Project | Commits | Files | TODO/FIXME (Δ) | Status |
|---|---:|---:|---|---|
| Maestro | 5 | 12 | 5 (+2) | ✅ active |
| arbiter | 3 | 27 | 3 (+3) | ✅ stabilized |
| atp-platform | 31 | 137 | 1 (−3) | ✅ high velocity |
| spec-runner | 0 | 0 | 16 (=) | ⏸️ frozen |
| proctor-a | 0 | 0 | 1 | ⏸️ paused (15d) |
| open-prose | 0 | 0 | 0 | ⏸️ spec-only (24d) |
| agents-for-game | n/a | 5+ | n/a | ⚠️ no VCS |
