---
title: Status check — AI orchestrator ecosystem
type: note
status: archived
owner: Andrei
updated: 2026-06-26
archived: 2026-07-06
reason: historical status snapshot (bulk-archived on graduation from _cowork_output)
---

# Status check — AI orchestrator ecosystem

> Date: 2026-06-26 (Friday)
> Method: `git log/status` across all repositories, grep TODO/FIXME, inspection of contract files (`agents.toml`, schemas, vendored pin) and their consumers.
> Baseline report for the delta: `_cowork_output/status/2026-06-19-status.md`.
> Mode: read-only; output only into `_cowork_output/`.

## TL;DR

1. **Maestro broke its 4-week silence — 9 commits and caught up with the loop.** Last week Maestro was the "🔴 laggard"; this week it adopted the agent-id convention `<harness>@<model>` (`#29`), pinned the executable models of the spawners to the same ones the R-07 sweep ran (`claude-sonnet-4-6` / `gpt-5.5`, `#32`), migrated `CodexSpawner` to `codex exec` (`#33`), and bumped the arbiter pin `7aeb6b1 → f3c955c` (`#30`, provenance-only).
2. **The main event of the week is a coordinated migration of the agent-id convention across all 3 cores.** `atp` (`#200`), `arbiter` (`#24`/`#26`), `Maestro` (`#29`) simultaneously switched to `<harness>@<model>`. All three converged on identical keys: `claude_code@claude-sonnet-4-6`, `codex_cli@gpt-5.5`. The identity of the routing key is now end-to-end: arbiter decides → Maestro executes the same model.
3. **arbiter ingested real R-07 data.** `#26` (`cb71b57`) loaded 13 ATP payloads from sweep-2026-06-21 into `benchmark_runs` via `report_benchmark`, re-keyed `config/agents.toml` to the collapsed models, and added `review` to codex's `supports_types`. `#27` — deterministic tie-break by `agent_id`.
4. **atp-platform — another 10 commits under R-07.** Roster Tier-1 (sonnet + mimo/qwen, opus retired `#201`), Tier-2 (pi@gpt-5 + opencode@glm-5.1, non-routable `#204`), structured code-review output + schema gate (`#199`), fix for infra failures of codex+mimo (`#202`), artifact corpus grounding (`#203`/`#207`).
5. **Contracts are in order, but two loose ends.** `report_benchmark` schema is **byte-for-byte identical** (arbiter↔atp), protocol `1.1.0` unchanged, DTO untouched. Loose ends: (a) `agents.toml` was re-keyed only for claude/codex — the `[aider]` section stayed in the old bare format; (b) Maestro pin `f3c955c` lags 3 commits behind arbiter HEAD `e1881c7`. The COWORK_CONTEXT drift is now in its **6th week**.

---

## 1. Project state (git, delta vs 2026-06-19)

| Project | Commits | Latest commit | When | Status |
|--------|----------|------------------|-------|--------|
| **atp-platform** | 10 | `0fc15b1` docs(R-07): CLI corpus grounding design (Path A) + AGENTS.md (#207) | 2026-06-21 | 🟢 R-07 roster + grading |
| **Maestro** | **9** | `6e1d4e3` fix(spawners): migrate CodexSpawner to `codex exec` (#33) | 2026-06-21 | 🟢 **silence broken, loop caught up** |
| **arbiter** | 4 | `e1881c7` fix(routing): deterministic ranking tie-break by agent_id (#27) | 2026-06-21 | 🟢 real R-07 data ingested |
| **spec-runner** | 0 | `2c43cc2` chore(release): v2.7.0 | 2026-06-14 | 🟢 stable (after 5 releases) |
| **spec-runner-tasks** | 0 | `f669e26` Merge task-020-shout (pi-demo) | 2026-06-12 | 🟡 dogfooding (1 uncommitted) |
| **proctor-a** | 0 | `53e8844` fix(tests): remove unused import | 2026-04-16 | ⏸ paused ~10 weeks (4 uncommitted) |
| **open-prose** | 0 | `6284194` plagin test | 2026-04-07 | 📄 spec-only (3 uncommitted in evaluation/) |
| **appgraph / prograph / prograph-vault** | — | no git | — | ⚠ not in the registry, **still without VCS** |
| **agents-for-game / spec-runner-test** | — | no git | — | sandbox without VCS (expected) |

### What's new in the code (summary)

- **Maestro** `#29` (`bce7f0f`): routing accepts arbiter ids `<harness>@<model>`; `#32` (`385dd90`): `DEFAULT_CLAUDE_MODEL="claude-sonnet-4-6"`, `DEFAULT_CODEX_MODEL="gpt-5.5"` (override via `MAESTRO_*_MODEL`) — "the executed model matches the routing decision"; `#33` (`6e1d4e3`): `codex exec` under codex CLI 0.139.x; `#30` (`b8de57d`): pin-bump provenance-only; +4 dependabot (starlette 1.3.1, cryptography 48.0.1, python-multipart 0.0.31, pyjwt 2.13.0).
- **arbiter** `#26` (`cb71b57`): `scripts/ingest_benchmark_payloads.py` + `ArbiterClient.report_benchmark` loaded 13 ATP payloads into `benchmark_runs` (idempotent by `run_id`); `config/agents.toml` re-keyed to `claude_code@claude-sonnet-4-6` / `codex_cli@gpt-5.5`; golden 08/09 updated; `scripts/ab_bench_rerank.py` runs `ARBITER_BENCH_WEIGHT 0 → 0.15`. `#27` (`e1881c7`): deterministic tie-break by `agent_id`. `#24`/`#25`: convention + sync README/spec.
- **atp-platform** `#199` (`c31c413`): structured code-review output (object findings + schema gate); `#201` (`94a5ea9`): weekend Tier-1 roster (sonnet + mimo/qwen, opus retired); `#202` (`0c43e7c`): unbreak codex + mimo (were 0.0 = infra fail); `#204`: Tier-2 pi@gpt-5 + opencode@glm-5.1 (CLI shims, non-routable); `#203`/`#207`: artifact corpus grounding (Path A).

> **TODO/FIXME:** a full-repo grep gives atp ~130 / arbiter ~24 / Maestro ~51 / spec-runner ~99 — but these absolutes are not comparable with the previous report (that one counted narrow markers, without templates/test). Directionally: the spec-runner spike (45→99) is still driven by template placeholders of the model-aware presets, not real debt. The metric should be locked to a single formula (`git grep -nE 'TODO|FIXME' -- '*.py' ':!*/templates/*' ':!*/tests/*'`), otherwise the comparison is meaningless.

---

## 2. Contract points

| Contract | File | Changes | Consumers updated? |
|----------|------|-----------|------------------------|
| **agent-id convention** `<harness>@<model>` | `arbiter/config/agents.toml`, `Maestro/maestro/spawners/*`, atp roster | arbiter re-keyed claude/codex (`#26`); Maestro accepts the format + pins the models (`#29`/`#32`); atp adopted (`#200`) | ✅ aligned on keys `claude_code@claude-sonnet-4-6`, `codex_cli@gpt-5.5` — but see ⚠ below |
| `report_benchmark` schema | `arbiter/arbiter-mcp/tests/contract/report_benchmark-v1.schema.json` vs `atp-platform/method/contract/report_benchmark-v1.schema.json` | — | ✅ **IDENTICAL** (empty diff) |
| MCP protocol version | `arbiter/arbiter-mcp/src/server.rs:418` | unchanged — `1.1.0` | ✅ compatible |
| route_task DTO | `arbiter/arbiter-mcp/src/route_task.rs` | tie-break (`#27`) — server-side logic, DTO untouched | ✅ backward-compatible |
| Maestro→arbiter vendored client | `Maestro/maestro/coordination/arbiter_client.py:65` | pin `f3c955c` (was `7aeb6b1`); protocol `1.1.0`, `MIN_ARBITER_PROTOCOL=(1,1)` | ⚠ pin lags **3 commits** behind HEAD `e1881c7` (`#25` docs, `#26` ingest+rekey, `#27` tie-break) |

**Main conclusion on contracts:** the transport contract is intact — DTO and protocol are unchanged, the `report_benchmark` schema is identical. The substantive change of the week is the **semantics of the routing keys** (`<harness>@<model>` + the specific models), and it is aligned: Maestro executes exactly the models on which arbiter makes its decision. This effectively closes last week's "vendoring drift" risk, although the formal pin still lags.

**Two mismatches:**
1. **`arbiter/config/agents.toml`: the convention migration is incomplete.** `claude_code@claude-sonnet-4-6` and `codex_cli@gpt-5.5` were re-keyed, but the `[aider]` section stayed in the old bare format (without `@<model>`). Asymmetric: either aider should also be under the convention, or explicitly document that it has no benchmark data and is intentionally outside the `<harness>@<model>` schema.
2. **R-07 A/B still gives no separation.** Both routable agents in `benchmark_runs` have a code-review score of `0.8` → re-rank applies equal deltas, the outcome is decided by the tie-break (`#27`). For a meaningful A/B a **second, separating benchmark** is needed (Phase 2 gate) — as was recorded in `2026-06-13-r07-phase1-arbiter-rerank-plan.md`.

---

## 3. Brief status

- **Progressed:** Maestro emerged from its 4-week pause and closed the consumer side of R-07 — it adopted the agent-id convention and pinned the executable models to the routing decisions; for the first time the agent keys are end-to-end across all 3 cores. arbiter ingested **real** sweep data into `benchmark_runs` (Phase 0 "no data" is finally lifted). atp keeps building grading/roster under R-07 (10 commits).
- **Blocked / idle:** R-07 A/B shows no agent separation (both 0.8 on code-review) — a separating benchmark #2 is needed (Phase 2). `ARBITER_BENCH_WEIGHT` is still gated (not enabled in prod; `ab_bench_rerank.py` runs 0→0.15 only in the script). R-06b **M5 (CLI)** is still pending. proctor-a — paused ~10 weeks.
- **Needs attention:** (1) `[aider]` in `agents.toml` is outside the new convention — remove the asymmetry. (2) The Maestro pin lags 3 commits (compatible, but the pin should not hang). (3) **COWORK_CONTEXT has not been updated since 2026-05-23 — 6th week**: the versions are stale (spec-runner is listed as 2.0.0 ↔ actually 2.7.0; atp 2.0.0 ↔ ≥2.1.0), the agent-id convention and R-07 data are not reflected in the integration map, `appgraph/prograph/prograph-vault` are not registered and **still without git**.

---

## Recommended actions

1. **arbiter (P1):** finish the convention migration — either re-key `[aider]` in `arbiter/config/agents.toml` to `<harness>@<model>`, or add a comment that aider is intentionally outside the schema (no benchmark data). Right now the section breaks the pattern.
2. **arbiter (P1):** add a **separating benchmark #2** (not code-review) to `benchmark_runs`, so that the A/B of `ARBITER_BENCH_WEIGHT` shows a real delta — without it Phase 1 is "proven on equal scores". Close R-06b **M5 (CLI)**.
3. **Maestro × arbiter (P1):** determine who sets `ARBITER_BENCH_WEIGHT>0` in e2e and when, and bump the pin `f3c955c → e1881c7` (the contract is compatible, minimal risk). Right now the loop is aligned by identifiers, but re-rank is disabled in prod.
4. **COWORK_CONTEXT (P0, 6th week):** update the registry — versions (spec-runner 2.7.0, atp ≥2.1.0), add the agent-id convention `<harness>@<model>` to the integration map and contract points, reflect the ingest of real R-07 data. Make a decision on `appgraph/prograph/prograph-vault` (register / mark as untracked) — all three are still without git.
5. **All projects (P2):** lock a single formula for counting TODO/FIXME (excluding `templates/` and `tests/`), otherwise the weekly debt delta is not comparable.

---

> Automated weekly status-check. Sources: `git log/status`, `git grep`, diff of contract files as of 2026-06-26.
