---
title: Status check — the AI-orchestrators ecosystem
type: note
status: archived
owner: Andrei
updated: 2026-06-19
archived: 2026-07-06
reason: historical status snapshot (bulk-archived on graduation from _cowork_output)
---

# Status check — the AI-orchestrators ecosystem

> Date: 2026-06-19 (Friday)
> Method: `git log/status` across all repositories, grep TODO/FIXME, checking contract files and their consumers.
> Baseline report for the delta: `_cowork_output/status/2026-06-12-status.md`.
> Mode: read-only; output only to `_cowork_output/`.

## TL;DR

1. **The "benchmark → routing" loop finally moved in CODE after 3 weeks of idling.** `arbiter` broke its 20-day silence and wired in **R-07 Phase 1 — benchmark-aware re-rank** (`arbiter@fbc06aa`): the `benchmark_runs` table, which M4 only wrote, is now read. Behavior is gated via `ARBITER_BENCH_WEIGHT` (default `0.0`) — byte-for-byte compatible with the old routing.
2. **`atp-platform` — a record week (~30 commits).** The grading spine for R-07 was built: uniform `CaseVerdict` + checker-registry, canonical dimensioned store (SP-1), dashboard leaderboard (SP-3), `task_type`+`language` in the case schema (SP-4), ollama local-model shim, ADR-006/007. Real code-review benchmark rows were generated — exactly what closed the Phase 0 "no data" for arbiter.
3. **`spec-runner` shipped 5 releases (v2.4.0 → v2.7.0):** model-aware templates (qwen/copilot presets), orphan-recovery on session interruption, `doctor` fix.
4. **The new laggard is `Maestro`: silence for 4 weeks (since 2026-05-23).** arbiter is 7 commits ahead of the SHA vendored in Maestro. The contract is still compatible, but the pin is stale and consumer-side R-07 integration hasn't started.
5. **Contracts are in order:** `report_benchmark-v1.schema.json` is identical in arbiter and atp; protocol is still `1.1.0` (re-rank without a DTO change). The COWORK_CONTEXT registry drift has been unresolved for a **5th week now**.

---

## 1. Project state (git)

| Project | Commits since 06-12 | Last commit | When | TODO/FIXME (was→now) | Status |
|--------|------------------|------------------|-------|--------------------------|--------|
| **atp-platform** | ~30 | `7d43810` R-07: pipe-check per-case axis-sweep dashboard (#197) | 2026-06-18 | 3 → **2** | 🟢 R-07 eval infrastructure |
| **arbiter** | 7 | `daa575f` docs: audit and sync (#23) | 2026-06-17 | 2 → **1** | 🟢 R-07 Phase 1 wired |
| **spec-runner** | 15 | `2c43cc2` chore(release): v2.7.0 | 2026-06-14 | 0 → **45** ⚠ | 🟢 5 releases (v2.4→v2.7) |
| **spec-runner-tasks** | 1 | `f669e26` Merge task-020-shout (pi-demo) | 2026-06-12 | — | 🟡 dogfooding (1 uncommitted) |
| **Maestro** | **0** | `6574d32` docs(changelog): PR #23 | 2026-05-23 | 2 → **5** | 🔴 4 weeks of silence (2 uncommitted) |
| **proctor-a** | 0 | `53e8844` fix(tests): remove unused import | 2026-04-16 | 0 → **0** | ⏸ paused 9 weeks (4 uncommitted) |
| **open-prose** | 0 | `6284194` plagin test | 2026-04-07 | — | 📄 spec-only, 2.5 months without commits |
| **appgraph** | — | no git | — | — | ⚠ not in the registry, no VCS |
| **prograph** | — | no git | — | — | ⚠ not in the registry, no VCS (has Cargo+uv) |
| **prograph-vault** | — | no git | — | — | ⚠ not in the registry, no VCS |
| **spec-runner-test** | — | no git | — | — | sandbox without VCS |

> ⚠ `spec-runner` TODO 0→45: the jump is almost certainly from added model-aware **templates** (qwen/copilot presets contain TODO placeholders in template strings), not real debt. Worth confirming with a separate grep excluding `templates/`.

### What's new in code (digest)

- **arbiter** `fbc06aa` (R-07 Phase 1): `db.rs::get_benchmark_score(agent_id, benchmark_id)` — task_type-scoped reader, latest-ts wins, clamp `[0,1]`; `route_task.rs::apply_benchmark_rerank` — `delta = (score - 0.5) * weight`, centered (a neutral/missing score doesn't move the decision), an audit line in `pred.path`. IT-08 A/B test. 169 lib+integration tests green. Plus 5 dependabot bumps (cryptography, starlette, pyjwt, python-multipart) and `307cfca` — unblocking the red Rust CI (fmt+clippy+vendor obs-contract).
- **atp-platform**: `c97cdab` uniform `CaseVerdict` + checker registry (grader spine), `99b8f01` canonical dimensioned store (SP-1), `682f3cf` dashboard leaderboard (SP-3), `5f35692` SP-4 — `task_type`/`language` into the case schema → columns in the DB, `64f1eb0` ollama spawner shim, `265a6f0` ADR-007 test-taxonomy, `3c59903` ADR-006.
- **spec-runner**: `5137a78` model-aware templates (`--model` now applied), `6d97e40` qwen/copilot presets, `1df077a` survive session interruption (orphan recovery), `cf9256e` fix parsing of claude cost from `--output-format json`.

---

## 2. Contract points

| Contract | File | Changes | Consumers updated? |
|----------|------|-----------|------------------------|
| `report_benchmark` schema | `arbiter/arbiter-mcp/tests/contract/report_benchmark-v1.schema.json` vs `atp-platform/method/contract/report_benchmark-v1.schema.json` | ATP touched it in SP-4 but keeps it in sync | ✅ **IDENTICAL** (empty diff) |
| MCP protocol version | `arbiter/arbiter-mcp/src/server.rs:418` | unchanged — `1.1.0` | ✅ compatible |
| arbiter route_task DTO | `arbiter/arbiter-mcp/src/route_task.rs` | R-07 re-rank **without a DTO change** (audit only in `pred.path`) | ✅ backward compatible |
| Maestro→arbiter vendored client | `Maestro/maestro/coordination/arbiter_client.py:59-61` | pin `ARBITER_VENDORED_FROM_SHA=7aeb6b1` (2026-05-23) | ⚠ **pin stale by 7 commits** (arbiter HEAD=`daa575f`) |
| ATP case schema | `atp-platform/method/agent-eval-case.schema.json`, `packages/atp-method/atp_method/schema.py` | SP-4: +`task_type`, +`language` (additive) | ✅ internal to ATP eval |

**Main takeaway on contracts:** arbiter moved the routing implementation forward but **deliberately without breaking the contract** — the DTO didn't change, protocol stayed `1.1.0`, behavior is gated default-off. So Maestro's vendored client (`7aeb6b1`) formally stays valid. The risk isn't incompatibility, it's **vendoring drift**: Maestro hasn't re-pulled the client in 4 weeks and has no integration coverage for benchmark-re-rank on its side.

---

## 3. Brief status

- **Progressed:** the R-07 loop closed technically for the first time — ATP generates real code-review benchmarks (SP-1..SP-4, grader spine), arbiter reads `benchmark_runs` and re-ranks agents (Phase 1, gated). spec-runner releases steadily (v2.7.0, model-aware templates). arbiter's CI (Rust) unblocked.
- **Blocked / idle:** Maestro — 4 weeks without commits; consumer-side R-07 integration (enable `ARBITER_BENCH_WEIGHT`, re-vendor the client, e2e route→outcome on real scores) hasn't started. R-06b **M5 (CLI)** still pending. proctor-a paused 9 weeks.
- **Needs attention:** (1) `ARBITER_BENCH_WEIGHT` defaults `0.0` — in prod the re-rank is effectively off; a decided enablement plan + Phase 2 gate (crossover/task-dependence on a 2nd benchmark) is needed. (2) COWORK_CONTEXT drift, 5th week: spec-runner listed as 2.0.0 (actually **2.7.0**), atp 2.0.0 (actually ≥2.1.0), `appgraph/prograph/prograph-vault` unregistered **and still without git**. (3) spec-runner TODO 0→45 — verify these are templates, not debt.

---

## Recommended actions

1. **Maestro (P0):** re-vendor the arbiter client from `7aeb6b1` → `daa575f` and update `ARBITER_VENDORED_FROM_SHA` + `test_arbiter_client_structure.py:15`. The contract is compatible, risk is minimal, but the pin shouldn't hang 7 commits behind.
2. **Maestro × arbiter (P0):** define the R-07 enablement plan — who sets `ARBITER_BENCH_WEIGHT>0` and when, an e2e test `route_task → report_outcome` on real `benchmark_runs`. Right now the loop is "proven on a synthetic seed", off in prod.
3. **arbiter (P1):** Phase 2 gate — a second benchmark (not code-review) to test task-dependence/crossover, as recorded in `2026-06-13-r07-phase1-arbiter-rerank-plan.md`. Close R-06b **M5 (CLI)**.
4. **COWORK_CONTEXT (P1, 5th week):** update the versions (spec-runner 2.7.0, atp ≥2.1.0); make a decision on `appgraph/prograph/prograph-vault` — register or explicitly mark as untracked; all three are still without git.
5. **spec-runner (P2):** confirm the nature of the 45 TODO/FIXME (`git grep -nE 'TODO|FIXME' -- '*.py' ':!*/templates/*'`); if these are template placeholders — exclude them from the debt metric.
