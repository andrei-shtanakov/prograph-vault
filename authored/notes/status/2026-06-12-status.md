---
title: Status Check — 2026-06-12
type: note
status: archived
owner: Andrei
updated: 2026-06-12
archived: 2026-07-06
reason: historical status snapshot (bulk-archived on graduation from _cowork_output)
---

# Status Check — 2026-06-12

> Weekly status check of the AI-orchestrators ecosystem.
> Window: 2026-06-05 → 2026-06-12. Comparison baseline: `_cowork_output/status/2026-06-05-status.md`.
> Method: `git log/status` across all repositories, grep TODO/FIXME, checking contract files.
> Mode: read-only, output only to `_cowork_output/status/`.

## TL;DR
1. **A breakthrough week for atp-platform: 34 commits, release v2.1.0.** The highlight — a new plugin `atp-method` (the agent-eval-case methodology run through the platform), on-prem / air-gapped LLM judge (OpenAI-compatible `base_url`, Bedrock provider, env steering), an AWS/EC2 demo, dashboard run-history (`/ui/executions`), plus security fixes (idna CVE-2026-45409, starlette 1.0.1, SAML-optional boot). The release is **additive, no breaking changes**.
2. **spec-runner shipped v2.3.1 and v2.4.0 (24 commits).** New: a `doctor` command (CLI/model compatibility probe + `doctor-result.schema.json`), pi-driven dev→review→test loop templates, a fix to claude cost-tracking (`--output-format json`), a fix to committing the DONE status of tasks, `plan --from-file`, tag-triggered PyPI publish.
3. **A working dogfooding loop appeared: `spec-runner-tasks` (16 commits).** A test project `textkit` (19 tasks: 12 independent + 3 dependency chains) was actually run through spec-runner — TASK-001..019 with auto-branches, merges, and code-review fixes. This is the first end-to-end confirmation of the pipeline on a live project.
4. **Maestro and arbiter have been silent for 20 days now (since 2026-05-23).** R-07 (eval-driven routing) still has not started in code — the "benchmark → routing" loop has been idle for a **third week** in a row after M4 unblocked it. R-06b M5 (CLI), Maestro M3 observability — still pending.
5. **Contracts are stable in this window:** the Maestro-facing `executor-state.schema.json` and `protocol/models.py` (ATP SDK) did NOT change; the new `doctor-result.schema.json` is internal, with no external consumers. The COWORK_CONTEXT registry drift is not closed (P1 for a fourth week): spec-runner is listed as 2.0.0 (actual 2.4.0), atp-platform 2.0.0 (actual 2.1.0), prograph/appgraph/prograph-vault not registered.

---

## Project status (window 2026-06-05 → 06-12)

| Project | Commits in window | Last commit | Date | Uncommitted | State |
|--------|---:|------------------|------|---:|-----------|
| **atp-platform** | 34 | `b84ef96` build(atp-method): PyPI publish workflow + correct floor (#165) | 2026-06-12 | 3 | 🟢 Release v2.1.0 + atp-method plugin |
| **spec-runner** | 24 | `2757899` ci: skip-existing on PyPI publish | 2026-06-12 | 0 | 🟢 Releases v2.3.1 + v2.4.0 (doctor) |
| **spec-runner-tasks** | 16 | `8c649ca` TASK-019: code review fixes | 2026-06-11 | 1 | 🟢 Dogfooding textkit through spec-runner |
| **Maestro** | 0 | `6574d32` docs(changelog): Copilot review PR #23 | 2026-05-23 | 2¹ | 🔴 20 days of silence after M4 |
| **arbiter** | 0 | `8270fc9` docs: Copilot review PR #16 | 2026-05-23 | 2 | 🔴 20 days of silence after M4 |
| **prograph / appgraph** (root repo) | 0 | `4f77ad2` appgraph: fix tasks.md spec validation | 2026-05-28 | 1² | 🟡 Silence; registry not updated |
| **proctor-a** | 0 | `53e8844` fix(tests): unused asyncio import | 2026-04-16 | 4 | 🔴 Paused 8+ wk |
| **open-prose** | 0 | `6284194` plagin test | 2026-04-07 | 3 | ⏸️ Idle 9+ wk |

> ¹ Maestro untracked still contains `docs/architecture/` (the two-mode arch doc outside git since the last report) + `.claude/settings.local.json`.
> ² The substantive item in the root repo: `M appgraph/spec/tasks.md` — uncommitted since 2026-05-28 (15 days). The rest of the `??` are nested repos and cowork files (normal).

### atp-platform v2.1.0 — what landed (34 commits)
- **Plugin `atp-method` (fully complete):** schema + loader (`agent-eval-case` → `TestDefinition`, `#144`), `AgentEvalCaseEvaluator` (critical_check + weighted rubric, `#145`), `register()` + source dispatch + E2E (`#146`). A suite format-dispatch registry replaced the hardcoded game branch (`#143`). Hard-gate critical assertions in scoring (`#142`).
- **On-prem / air-gapped judge:** OpenAI-compatible `base_url` for the LLM judge (`#149`), a Bedrock-hosted Claude provider (`#136`), env steering of provider/region (`#152`), a turnkey air-gapped grader that reuses the agent's local model (`#150`).
- **Demos:** on-prem docker-compose (`#135`), an AWS cloud variant (EC2 + Bedrock via IAM, `#153`), a unified EPAM overview/runbook (`#137`, `#155`).
- **Dashboard:** `/ui/executions` run-history (list + detail, `#157`), an adapter/model column on suite executions (`#159`), a fix to success semantics (evaluation pass, not execution, `#162`).
- **Security/ops:** idna 3.11→3.15 (CVE-2026-45409, `#133`), starlette 0.50→1.0.1 (`#131`), SAML-optional boot (`#161`), RBAC roles persist (`#160`).

### spec-runner v2.3.1 + v2.4.0 — what landed (24 commits)
- **`doctor` (`#14`):** a CLI/model compatibility probe + a new `schemas/doctor-result.schema.json` + 7 fixtures. A direct response to the discovered cost-tracking breakage.
- **Fixes:** claude cost-tracking restored via parsing of `--output-format json` (`#16`); committing a task's DONE status now writes `tasks.md` BEFORE the commit, not after the merge (`9f62ab1` — a bug surfaced by the textkit dogfooding run).
- **Features:** `plan --from-file` (`#17`), pi-driven dev→review→test loop templates + guide (`2c78a2c`, `7b50790`).
- **Ops:** tag-triggered PyPI publish + skip-existing idempotency, ruff format (fix of the v2.3.0 lint drift).

---

## Contract points

| Contract | Changes in window | Consumers |
|----------|------------------|-------------|
| **spec-runner `schemas/executor-state.schema.json`** (Maestro-facing) | ✅ **No changes** in this window | Maestro `ExecutorState` (`extra="ignore"`) is stable. ⚠ Debt since v2.3.0 (`error_kind`/`error_stage`) — the fixtures `maestro-interop/` are still NOT synchronized |
| **spec-runner `schemas/doctor-result.schema.json`** | 🆕 **New** (`79d4607`, v2.4.0, +28 lines) | Internal contract of the `doctor` command; no external consumers |
| **atp-platform `packages/atp-core/atp/protocol/models.py`** (MCP/SDK) | ✅ **No changes** in the window | pin held; v2.1.0 is additive |
| **atp-platform `atp-method` schema** (`agent-eval-case.schema.json`) | 🆕 New plugin format | Internal to atp-method; does not overlap with Maestro↔ATP |
| arbiter MCP (6 tools, protocol 1.1.0) | ✅ No changes | pin held |
| `arbiter/config/agents.toml` (02-07), `arbiter-core/src/types.rs` (04-05) | ✅ No changes | ✅ frozen |
| Observability `log-schema.json` / `obs.py @ fa6b106` | ✅ No changes | 4-way axis aligned |

**Contract conclusion:** cross-project contracts are stable in this window — no Maestro-facing/ATP-SDK file was touched, and the atp v2.1.0 and spec-runner v2.4.0 releases are additive. The new schemas (`doctor-result`, `agent-eval-case`) are internal, with no consumers in other repositories. **The only unresolved contract debt remains the same:** the additive evolution of executor-state in spec-runner v2.3.0 is not reflected in the Maestro interop fixtures. The classified failure reasons (`error_kind`) still sit as a ready signal for R-07.

### TODO/FIXME
Genuine code debt unchanged — the same 3 items: `arbiter/orchestrator/arbiter_client.py:78` (pyrefly NamedTemporaryFile), `atp-platform/atp/cli/commands/push.py:122`, `Maestro/tests/test_git_worktree.py:316` (merge CONFLICT in stdout). The high grep counts for spec-runner (95) are `.claude/` templates (41) and test fixtures (36), not code debt.

---

## Brief status

- **What progressed:** atp-platform — a strong release v2.1.0 (the atp-method plugin, on-prem/air-gapped judge, an AWS demo, dashboard run-history, security fixes). spec-runner — v2.3.1 + v2.4.0 (`doctor`, pi-loop templates, a cost-tracking and DONE-commit fix). A real dogfooding loop was launched: textkit (19 tasks) run through spec-runner in `spec-runner-tasks`, and that run already exposed and closed two spec-runner bugs.
- **What is blocked / idle:** Maestro and arbiter — 20 days without commits. R-07 (eval-driven routing) has not started for a third week; the benchmark→routing loop is not closed. R-06b M5 (CLI `maestro benchmark`) and Maestro M3 observability — pending.
- **What needs attention:** (1) the COWORK_CONTEXT registry is 2 versions behind for spec-runner and 1 for atp-platform, prograph/appgraph/prograph-vault not registered — P1 for a fourth week; (2) the executor-state v2.3.0 drift vs maestro-interop fixtures is not closed; (3) Maestro `docs/architecture/` is still outside git; (4) proctor-a → archived (fourth week of the flag); (5) `appgraph/spec/tasks.md` uncommitted for 15 days.

---

## Recommended actions

| Priority | Project | Action | Rationale |
|-----------|--------|----------|-------------|
| **P1** | COWORK_CONTEXT.md | Update versions: spec-runner 2.0.0→**2.4.0**, atp-platform 2.0.0→**2.1.0**; add prograph/appgraph/prograph-vault to the registry | The registry is already 4 reports behind; the gap grew (spec-runner +0.4, atp +0.1) |
| **P1** | Maestro/arbiter (R-07) | Start eval-driven routing; wire `error_kind` from spec-runner + `BenchmarkResult` as inputs to the DT | 20 days of idle on the unblocked loop — the main systemic coherence risk |
| **P2** | Maestro ↔ spec-runner | Close the R-04 drift: synchronize the `maestro-interop` fixtures to the v2.3.0+ fields or explicitly document the ignoring of experimental fields | The "frozen" contract is growing one-sidedly since v2.3.0 |
| **P2** | Maestro | Commit `docs/architecture/` (the two-mode arch doc) | Architecture document outside VCS for a second report in a row |
| **P2** | proctor-a | Move to `📦 archived` in the registry | 8+ wk paused; flag since 2026-05-22 |
| **P3** | appgraph | Commit or revert `spec/tasks.md` (hanging since 2026-05-28) | Uncommitted spec change in the root repo, 15 days |

---

*Sources: `git log/status` across atp-platform, spec-runner, spec-runner-tasks, Maestro, arbiter, proctor-a, open-prose, the root repo (prograph/appgraph); `atp-platform@167ba2d` (v2.1.0 release), `spec-runner@41e00f8` (v2.4.0 release), `spec-runner@79d4607` (doctor-result schema); check of `executor-state.schema.json` and `protocol/models.py` (no changes in the window); `COWORK_CONTEXT.md`; `_cowork_output/status/2026-06-05-status.md`.*
