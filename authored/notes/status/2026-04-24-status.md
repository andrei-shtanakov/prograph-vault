---
title: Weekly Status Check — 2026-04-24
type: note
status: archived
owner: Andrei
updated: 2026-04-24
archived: 2026-07-06
reason: historical status snapshot (bulk-archived on graduation from _cowork_output)
---

# Weekly Status Check — 2026-04-24

> Period: 2026-04-18 (previous report) → 2026-04-24 (6 days)
> Scope: Maestro, arbiter, atp-platform, proctor-a, spec-runner, open-prose, agents-for-game
> Mode: READ-ONLY (automated scheduled-run)
> Source: `git log --since="2026-04-18"`, `grep TODO|FIXME|HACK`, cross-checked with `_cowork_output/status/2026-04-18-status.md`, `COWORK_CONTEXT.md`, `Maestro/CHANGELOG.md`, Maestro `_cowork_output/observability-contract/`, `arbiter/TODO.md`.

## TL;DR

1. **A new cross-project axis — observability v1**. Over the week Maestro and spec-runner raised a shared log contract based on the OpenTelemetry Logs Data Model + W3C TraceContext (`TRACEPARENT`). Milestone **M1** is closed (Maestro `279193e` 2026-04-19): a single `TraceId` flows through the pipeline Maestro → real spec-runner subprocess, `merge-logs` assembles a time-sorted `merged.jsonl`. The contract artifacts live in `Maestro/_cowork_output/observability-contract/` (JSON Schema, propagation.md, 4 fixtures). spec-runner did a cutover `logging.py → obs.py shim` (`641b9b8`), Maestro vendored `obs.py` with the same discipline as `arbiter_client.py`.
2. **arbiter — 0 commits over 7 days** (last `7e6de56` 2026-04-17). The stabilization phase continues, **but `arbiter#9` (P0 from the last report) is not closed**: the `route_task` response still does not contain `metadata.decision_id` → the authoritative retry/abandon flow in the real arbiter binary is unobservable. This still blocks **arbiter R-05** (integration tests with a real subprocess) and Maestro v0.3.0 prep.
3. **atp-platform — 54 commits across three parallel themes**: (a) LABS-TSA "Tournament Sandbox Agent" — 7 PRs (#74–#81: data model → quota/purpose on the agent API → purpose-based MCP/benchmark gating → builtin participants → UI + match linkage → post-merge review security fix); (b) admin tournament GUI landed via PR #58 (landing `/ui/admin`, list, detail shell, create form, kick button, HTMX polling + post-mortem); (c) El Farol dashboard — LABS-97/99/102 + Cards variant (`d6841c1`, `502fe18`).
4. **proctor-a — 0 commits** over 8 days after the previous high-velocity sprint (91/8 days). A pause after PR #8 LABS-68 (NATS) landing. Not a blocker, but a strong contrast.
5. **open-prose — no movement for 17 days**, still the same `6284194 plagin test`. Recorded as a quiet artifact.
6. **agents-for-game (NOT in the registry) — an extension appeared**: a new file `claude_el_farol_agents.py` (Apr 24) + `prompt.py`, `.env.example.el_parol`, `.env.example.prisoner` — an explicit fork of `claude_pd_agents.py` toward El Farol via ATP MCP. An ATP-SDK client, it hooks into `ATP_MCP_URL`/`TOURNAMENT_ID`/`JOIN_TOKEN`. Still not a git repository, still not in `COWORK_CONTEXT.md`. This is obviously a proving ground for LABS-TSA on the ATP side — but there's no formal link between them.
7. **Contract points over the week**: (a) `arbiter/arbiter-core/src/types.rs`, `arbiter/arbiter-mcp/src/tools/*`, `Maestro/maestro/coordination/*` — **unchanged** (frozen after v0.2.0 / macos-arm64 artifact). (b) **New contract** — `observability-contract/log-schema.json` v1, a shared axis for Maestro ↔ spec-runner (future: arbiter Rust + Python client, ATP → Plan 2 / M2). Consumers synchronized: spec-runner `@fa6b106`, Maestro re-vendor `4688633` → `maestro/_vendor/obs.py`.
8. **TODO/FIXME — steady**: Maestro 3, arbiter 0, proctor-a 0, open-prose 0, spec-runner 16 (no movement), atp-platform 4 (+1 vs last report against 148 changed files — normal).

---

## 1. Per-project snapshot

### Maestro
- **Last commit:** `279193e milestone(obs): M1 reached — cross-process trace continuity` (2026-04-19)
- **Version (pyproject):** 0.2.0 (no bump; v0.3.0 prep not yet started)
- **Commits since 2026-04-18:** 10
- **Files changed:** 33 (including 20+ under `_cowork_output/` — contract artifacts)
- **TODO/FIXME/HACK (excluding tests):** 3 (unchanged)
- **Key themes:**
  - **Observability v1 — Plan 1 → M1** (new axis).
    - `c5e1a9f docs(obs): observability v1 design, review, and plan-1-to-M1` — design alignment.
    - `be29b16 feat(obs): contract v1 — JSON Schema + propagation.md + fixtures` — contract artifacts in `Maestro/_cowork_output/observability-contract/` (log-schema.json, propagation.md, 4 fixtures: root-span, nested-span, error-chain, redacted).
    - `66d7003 feat(obs): vendor obs.py from spec-runner@641b9b8` → `maestro/_vendor/obs.py` (8737 bytes).
    - `c5a3b00 feat(obs): Maestro init_logging + child_env in subprocess spawning`.
    - `cc8c89d feat(obs): standalone merge-logs CLI with tolerant parser` → `maestro/merge_logs.py`.
    - `62f5f58 feat(obs): auto-merge logs in pipeline finally block`.
    - `e3feefd test(obs): cross-process trace continuity (M1)` → `tests/test_obs_integration.py` + `tests/_obs_child.py`.
    - `4688633 chore(obs): re-vendor obs.py from spec-runner@fa6b106` — synchronization after `fix(obs)` in spec-runner.
    - `279193e milestone(obs): M1 reached` — TraceId `979b4685d0362bebfbdcc5f2fd459a2b` confirmed across 7 records via `test_trace_continuity_across_subprocess`.
  - **R-03 / coordination — unchanged** (`maestro/coordination/` frozen after v0.2.0, last change Apr 17).
  - **New architecture documentation**: `SUGGESTIONS.md` (new file at the root — 5 bottlenecks of the project: scope overlap, recovery naivety, worktree cleanup, hardcoded cost pricing), 9 reports in `_cowork_output/` (01-architecture-map, 02-dag-scheduler-analysis, 03-agent-integration, 04-code-quality-ops, 05-architecture-improvements, 06-roadmap, 07-competitive-analysis, 08-tasks, 09-r03-arbiter-mcp-client-review).
- **Contract note**: observability M1 *demonstrates* cross-process trace continuity only through an integration test (`tests/_obs_child.py` as a stand-in for spec-runner). The live pipeline `examples/hello.yaml` (`01KPK9TNBXKQGXSQ5X96HNS5VQ`) produces 0-byte JSONL — the scheduler internals use stdlib `logging`, not `obs.span()`/`obs.get_logger()`. That is, the wiring up to the CLI entry point exists, but the scheduler's internals are not yet instrumented. This is an honest caveat in `obs-m1-reached-2026-04-19.md`.
- **Blockers:** external `arbiter#9` (still no response), blocks v0.3.0 prep.

### arbiter
- **Last commit:** `7e6de56 ci(release-binary): add macos-arm64 build alongside linux-x64` (2026-04-17) — **unchanged for 7 days**.
- **Version:** 0.1.0 (no bump).
- **Commits:** **0** (previous window: 8).
- **Files changed:** 0.
- **TODO/FIXME/HACK:** 0 (unchanged).
- **Key themes:** nothing.
- **`arbiter/TODO.md`:** `R-05` (integration tests with a real subprocess) is still `[ ]`.
- **Blockers (external P0 carried over from the last report):** `arbiter#9` — the `route_task` response does not return `metadata.decision_id`, which breaks the observability authoritative-retry flow on the Maestro side when using the real binary. Over the week — zero reaction. A critical item that must be raised.

### atp-platform
- **Last commit:** `d1ff895 fix(tsa): close IDOR + race + builtin-key issues from post-merge review (#81)` (2026-04-24, ~72 minutes ago)
- **Commits:** 54 (previous window: 259 — a normal decline after the explosion)
- **Files changed (unique, lifetime diff):** 148
- **TODO/FIXME/HACK:** 4 (previous window: 3 — **+1**)
- **Key themes (by PR groups):**
  - **LABS-TSA — "Tournament Sandbox Agent" (a full pipeline of 7 PRs):**
    - `9d8efb1 docs: tournament agent sandbox — spec + implementation plan (LABS-TSA) (#74)` — specification and plan.
    - `b5c8725 feat(db): tournament agent sandbox — data model + migration (PR-1) (#75)` — migration `853688412c5b_tournament_agent_sandbox_labs_tsa_pr_1.py` + `a9c4e81f3d2a_tournament_participant_agent_xor_builtin_check.py`.
    - `64dcb72 feat(api): quota + purpose on agent API (LABS-TSA PR-2) (#77)` — agent-level quota/purpose.
    - `d0f11e2 feat(auth): purpose-based MCP / benchmark gating (LABS-TSA PR-3) (#78)` — migration `52987a83afb7_apitoken_agent_purpose_labs_tsa_pr_3.py`, `mcp/auth.py` middleware.
    - `c5e7c83 feat(tournament): builtin participants + runner branching (LABS-TSA PR-4) (#79)` — `tournament/builtins.py`, `tournament/errors.py`.
    - `eebf913 feat(ui): tournament agent sandbox UI + match linkage (LABS-TSA PR-5) (#80)`.
    - `d1ff895 fix(tsa): close IDOR + race + builtin-key issues from post-merge review (#81)` — a post-merge security patch (IDOR + race + builtin-key).
  - **Admin tournament GUI (PR #58 + surrounding work, 13 commits):** `4a2d8bd feat(admin): /ui/admin landing + tournaments list`, `3ecb6a2 feat(admin): create-tournament form`, `7a396da feat(admin): tournament detail shell with Cancel button`, `8864448 feat(tournament): TournamentService.get_admin_activity snapshot`, `35143ee feat(admin): activity fragment endpoint + table + heatmap`, `c6b1361 feat(admin): HTMX polling + post-mortem variant`, `6dc5c5e feat(tournament): kick_participant sets released_at + timeout fallback`, `ebbb997 feat(api): DELETE /api/v1/tournaments/{id}/participants/{pid}`, `7c62701 feat(admin): Kick button per participant`, `f8cdc30/76b3803 feat(auth): admin-aware JWT TTL via ATP_ADMIN_TOKEN_EXPIRE_MINUTES`, `fd7bbc9 fix: address Copilot review on PR #58 (6 comments)`.
  - **El Farol dashboard:** `2edadae El Farol game dashboard data model (#63)`, `949500f docs(adr): ADR-005 El Farol dashboard — hybrid Jinja + scoped JS bundle (#64)`, `6b5b02e docs: El Farol dashboard mockups (LABS-97) (#65)`, `fc7e600 feat(dashboard): El Farol match dashboard endpoint (LABS-99) (#68)`, `d6841c1 feat(ui): El Farol Cards dashboard at /ui/matches/{id} (LABS-102) (#69)`, `9b6b954 feat(ui): /ui/matches listing + sidebar (#70)`, `502fe18 fix(ui): improve dashboard styles (#76)`.
  - **LABS-54 Phase 3** (Agent.owner_id NOT NULL): `f753cd6 feat(migration): Agent.owner_id NOT NULL, drop ownerless partial index`, `464a90f refactor(agents): delete ownerless code paths + inject owner_id in fixtures`.
  - **DB robustness:** `6fe0ecb fix(db): delegate column DDL to SQLAlchemy compiler + handle non-constant defaults`, `f35e5ef fix(db): quote string server_defaults in _add_missing_columns`, `ed0b334 fix(dashboard): init_database() falls back to ATP_DATABASE_URL env`.
  - **Docs / housekeeping:** `ff68307 docs: wire ATP into ecosystem roadmap + refresh monorepo structure`, `0bc2850 docs: add GitHub OAuth/Device Flow and CLIAdapter bugfix plans`, `140f370 chore(ci): make atp-sdk publish idempotent via skip-existing`, `2c5c8b5 chore(gitignore): ignore cross-project tooling artefacts and local notes`, `c2f4bdd refactor: move macOS launchers to scripts/macos, rules to docs/games/rules`, `69d0c64 docs(rules): add El Farol Bar rules (English and Russian)`.
- **Blockers:** none.

### proctor-a
- **Last commit:** `53e8844 fix(tests): remove unused asyncio import in test_bus.py` (2026-04-16) — **unchanged for 8 days**.
- **Commits:** **0** (previous window: 91 — a sharp pause after LABS-68 landing).
- **Files changed:** 0.
- **TODO/FIXME/HACK:** 0 (unchanged).
- **Key themes:** nothing.
- **Blockers:** none (possibly the team is on the next project, or the NATS transport is running in a product trial without code noise).

### spec-runner
- **Last commit:** `fa6b106 fix(obs): use TRACEPARENT parent span_id as initial _span_id` (2026-04-19)
- **Version:** 2.0.0 (no bump; last week's R-04 freeze is in effect).
- **Commits:** 11
- **Files changed:** 7 (all within `src/spec_runner/` + `tests/`, no new architecture — only the `logging.py → obs.py` cutover).
- **TODO/FIXME/HACK:** 16 (unchanged).
- **Key themes — observability v1 reference implementation:**
  - `ead7070 feat(obs): init_logging and get_logger skeleton` — the skeleton.
  - `788b77f feat(obs): TRACEPARENT parsing with graceful fallback` — W3C Trace Context inbound.
  - `31e4cdd feat(obs): span context manager with error chains`.
  - `b07153b feat(obs): redaction processor with default + env-extended blocklist` — secrets protection.
  - `1cd18f9 feat(obs): child_env() for subprocess trace propagation` — outbound `TRACEPARENT`.
  - `1bcf9eb test(obs): contract validation against shared schema and fixtures` — validation against `log-schema.json` and 4 fixtures.
  - `641b9b8 refactor(obs): cutover logging.py to obs.py shim` — `logging.py` shrunk from 97 to 20 lines, existing callers don't break.
  - `52c3ce6 style(obs): ruff format + lint cleanup`, `208938c test(obs): timestamp format (ns-string + ISO micros)`, `b36255a Logging` (housekeeping import).
  - `fa6b106 fix(obs): use TRACEPARENT parent span_id as initial _span_id` — this exact fix forced Maestro to re-vendor (see Maestro `4688633`).
- **Contract pin:** Maestro vendors `obs.py` at `spec-runner@fa6b106`.
- **Blockers:** none.

### open-prose
- **Last commit:** `6284194 plagin test` (2026-04-07) — **unchanged for 17 days**.
- **Commits:** 0.
- **Registry status:** 📄 Spec Only — matches reality.
- **Blockers:** not applicable (marked as spec-only).

### agents-for-game (still NOT in the registry)
- **NOT a git repository** — `.git/` missing, `.gitignore` missing, CLAUDE.md/README.md missing.
- **Changes since 2026-04-18:** 3 new files + a `.env` update:
  - `claude_el_farol_agents.py` (3620B, 2026-04-24) — 2 Claude SDK agents play **El Farol via ATP MCP** (tournament_id, join_token, alice/bob tokens, num_slots=16, capacity=2).
  - `prompt.py` (1912B, 2026-04-24) — dedicated prompts for Alice/Bob with an "ADAPTIVE EARLY BIRD" strategy (pick least-crowded slots based on attendance_history).
  - `.env.example.el_parol` (174B), `.env.example.prisoner` (174B), `.env.prisoner` (502B), a new `.env` (502B).
- **Context:** this is a direct forked client of ATP LABS-TSA (`claude_pd_agents.py` was for Prisoner's Dilemma → `claude_el_farol_agents.py` for El Farol). Both are live game_types from the past atp-platform reports.
- **Risk/recommendation (repeated from the last report):** either into the repository (an atp-platform subdirectory `games/` or a separate project "atp-playground"), or under `.gitignore`. As it stands — a neutral folder with secrets in `.env`, without history. `.env` with real tokens is a leak risk on any `cp -r` into a public resource.

---

## 2. Contract points — summary

| Contract file / group | Changed this week? | Last change | Consumers | Synchronized? |
|---|---|---|---|---|
| `arbiter/arbiter-core/src/types.rs` | ❌ | `6de778a` 2026-04-05 | arbiter-mcp, Maestro (vendored `861534e`) | ✅ frozen; **but `arbiter#9` P0 still hangs** |
| `arbiter/arbiter-mcp/src/tools/*` (5 tools) | ❌ | no commits | Maestro consumes 2 of 5 | ⚠️ unchanged since last week |
| `arbiter/config/agents.toml` | ❌ | `b18720d` 2026-02-07 | arbiter runtime | ok |
| `arbiter/config/invariants.toml` | ❌ | `96198c3` 2026-01 | arbiter runtime | ok |
| `Maestro/maestro/coordination/*` (arbiter_client/routing/errors) | ❌ | Apr 17 | Maestro scheduler + recovery | ✅ frozen after v0.2.0 |
| `Maestro/maestro/models.py` | ❌ | Apr 17 | scheduler, DB | ✅ frozen |
| `proctor-a/src/proctor/core/models.py`, `config.py`, `transport/*` | ❌ | `07ea941` / `59ac488` / `544ba2c` — 8-9 days ago | proctor runtime, GHA integration-nats | ✅ stable (week-old) |
| `spec-runner/schemas/` (executor-state, json-result) | ❌ | R-04 freeze `273ef00` from 2026-04-17 | Maestro Pydantic | ✅ frozen; CI catches breaking |
| `spec-runner/src/spec_runner/obs.py`, `logging.py` | ✅ **NEW** | `fa6b106` 2026-04-19 | Maestro (vendored at `fa6b106`) | ✅ — Maestro re-vendored in `4688633` |
| **`Maestro/_cowork_output/observability-contract/log-schema.json` + fixtures + propagation.md** | ✅ **NEW (didn't exist a week ago)** | `be29b16` 2026-04-19 | spec-runner contract tests + Maestro vendored obs.py + future arbiter/ATP | ✅ spec-runner `test_obs_contract.py` validates against this schema |
| `atp/evaluators/guardrails.py` | ❌ | `4648cfd` 2026-04-17 | external ATP SDK | ✅ |
| `atp-platform/packages/atp-dashboard/atp/dashboard/mcp/auth.py` + `schemas.py` | ✅ (LABS-TSA PR-3) | `d0f11e2` | external agents (ATP MCP clients) | ⚠️ **contract change** — a new `purpose` field on API tokens, gating `/mcp/*` and benchmark endpoints. External clients (agents-for-game, benchmark participants) must be able to authorize with a covered purpose. Consumers in our registry: **only `agents-for-game/`** — there the tokens have no purpose, a breakage risk under the migrated auth middleware. |

### Wiring check (grep)

- Maestro observability wiring: `grep -l "obs\.\|init_logging\|merge_logs" Maestro/maestro/**/*.py` → `cli.py`, `orchestrator.py`, `_vendor/obs.py`, `merge_logs.py` (4 files). **The contract axis v1 works.**
- Maestro arbiter wiring: 9 files (unchanged since last week).
- `arbiter/arbiter-mcp/src/tools/` — 5 tools, Maestro consumes `route_task` + `report_outcome`. **Unchanged.**
- **ATP → agents-for-game contract change:** LABS-TSA PR-3 (`d0f11e2`) introduces `purpose` gating. If `agents-for-game/claude_el_farol_agents.py` uses a token without a correct `purpose`, its `/mcp/*` calls may start returning 403. Recommendation: check before the next live run.

### Protocol risk (P0, carried over from the last report)

`arbiter#9`: the `route_task` response does not return `metadata.decision_id` → `arbiter_decision_id = NULL` in Maestro's SQLite → `_try_report_outcome` behaves as it would for static-routed → the authoritative retry / abandon flow is unobservable in real runs. **Maestro's 1118 tests are green** because `FakeArbiterClient` always provides `decision_id`.

**Over the week — zero progress:** arbiter had no commits, `R-05` (integration tests with a real subprocess) is still `[ ]` in `TODO.md`. This is silently accumulating debt: the longer Maestro lives with `FakeArbiterClient`, the deeper the fake becomes the "default contract."

---

## 3. Brief status (5–7 lines)

- **Progressed:** Observability v1 — a new cross-project axis Maestro ↔ spec-runner. M1 closed in 5 days: contract (JSON Schema + propagation + 4 fixtures), reference obs.py in spec-runner, cutover `logging.py → obs.py shim`, vendor in Maestro, merge-logs CLI, cross-process integration test, re-vendor after the fix in spec-runner. atp-platform closed three parallel themes: LABS-TSA pipeline (7 PRs), Admin tournament GUI, El Farol dashboard. All 54 commits over 6 days.
- **Blocked / slipping:** `arbiter#9` (P0 from last week) is not moving — arbiter had 0 commits over 7 days. `R-05` is still `[ ]`. `Maestro v0.3.0` prep depends on it. `proctor-a` — 0 commits over 8 days (from 91 to 0 — a sharp contrast). `open-prose` quiet for 17 days. `agents-for-game` 13 days outside the registry and now outside the purpose-based auth gating from LABS-TSA PR-3.
- **Needs attention (P0):** (1) Raise `arbiter#9` manually — write a minimal repro + PR into `arbiter-mcp/src/tools/route_task.rs` (or decide "this is actually the contract" and update `FakeArbiterClient` + Maestro's vendored client). A week of silence on a P0 is bad. (2) Check whether LABS-TSA PR-3 purpose-gating will break the external client `agents-for-game/`. If the tokens have no purpose, `/mcp/*` will be 403.
- **Needs attention (P1):** (3) Maestro M1 wiring honestly documented that the live pipeline produces 0-byte JSONL — the scheduler internals are still on stdlib `logging`. M2 or Plan-2 must close the real instrumentation of the scheduler pathway. (4) `COWORK_CONTEXT.md` still: describes 3 MCP tools instead of 5, marks Maestro↔Arbiter as 🔴 NOT IMPLEMENTED (in fact closed in v0.2.0), does not contain `agents-for-game/`, does not contain the new contract axis `observability-contract/` in Maestro's `_cowork_output/`. The registry has lagged for a second week now.
- **Needs attention (P2):** (5) `agents-for-game/` — decide its fate: an atp-playground subdirectory, a separate repo, or under `.gitignore`. Currently `.env` files with real tokens sit without git history — a bad habit. (6) `proctor-a` after NATS — check whether this was a real pause or the repository just isn't being published. Nothing breaks internally, but ecosystem monitoring should know.
- **Observation:** The ecosystem's pace over the week — Maestro 10 / arbiter 0 / atp-platform 54 / proctor-a 0 / spec-runner 11 / open-prose 0 = **75 commits** against the previous **430** (Maestro 62 + arbiter 8 + atp-platform 259 + proctor-a 91 + spec-runner 10). A big drop — but it's logical: last week closed 3 large refactors (R-03, R-10, LABS-68) + the tournaments explosion. Now it's a consolidation phase + a transition to a new "front" (observability, TSA).

---

## Recommended actions

1. **arbiter — unfreeze `arbiter#9` (P0, carried over a second week in a row).** Minimal repro: `cargo run -p arbiter-mcp -- -c config/` + `echo '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"route_task","arguments":{...}}}' | ...` → check whether `result.metadata.decision_id` is in the response. If not — fix in `arbiter/arbiter-mcp/src/tools/route_task.rs` (add `metadata: json!({"decision_id": id})` to the response) + bump the MCP API + a coordinated release with Maestro. If "this is the contract" — update `Maestro/maestro/coordination/arbiter_client.py:_extract_decision_id` for a fallback path and write it explicitly into `arbiter/docs/`. **Anchor:** `arbiter/arbiter-mcp/src/tools/route_task.rs`, `arbiter/TODO.md:53` (R-05), `Maestro/maestro/coordination/routing.py:_extract_decision_id`.

2. **atp-platform ↔ agents-for-game — synchronize purpose-gating.** LABS-TSA PR-3 (`d0f11e2 feat(auth): purpose-based MCP / benchmark gating`) introduces `purpose` on API tokens. Check whether `agents-for-game/claude_el_farol_agents.py` uses the current token format `TOKEN_ALICE`/`TOKEN_BOB`. If a migration is needed — update `.env.example.el_parol` with the new field `TOKEN_PURPOSE=tournament_play` (or whatever the naming is in PR-3). **Anchor:** `atp-platform/packages/atp-dashboard/atp/dashboard/mcp/auth.py`, `agents-for-game/claude_el_farol_agents.py`, `agents-for-game/.env.example.el_parol`.

3. **COWORK_CONTEXT.md — synchronize with reality (carried over from last week, already P0).** Changes to make:
   - Date 2026-04-16 → 2026-04-24.
   - Remove "🔴 NOT IMPLEMENTED" from Maestro↔Arbiter; add a link to Maestro v0.2.0.
   - 3 arbiter MCP tools → 5 (`route_task`, `report_outcome`, `agent_status`, `get_metrics`, `get_budget`).
   - **New "Contract points" section**: `Maestro/_cowork_output/observability-contract/` — Maestro + spec-runner, the path to M2 will add an arbiter Rust client + ATP. Pin: `log-schema.json` v1, spec-runner@`fa6b106`, Maestro vendored at `4688633`.
   - Registry `agents-for-game/` — either add it as experimental (roles: "external ATP MCP client for el_farol/PD"), or delete it.
   - Mark the spec-runner R-04 freeze in the contracts section.
   - **Anchor:** `all_ai_orchestrators/COWORK_CONTEXT.md:1-164`.

4. **Maestro — the M2 plan must close the scheduler-pathway instrumentation.** `obs-m1-reached-2026-04-19.md` honestly admits: the live `examples/hello.yaml` produces 0-byte JSONL. M2 must replace the stdlib `logging` calls inside `maestro/scheduler.py`, `maestro/orchestrator.py`, `maestro/recovery.py` with `obs.get_logger()` + `obs.span()`. Without this, "live pipeline observability" is only wiring-level fiction. **Anchor:** `Maestro/maestro/scheduler.py`, `Maestro/maestro/orchestrator.py`, `Maestro/maestro/_vendor/obs.py`, `Maestro/_cowork_output/plans/2026-04-19-observability-plan-1-to-m1.md` (a follow-up plan-2-to-M2 is needed).

5. **arbiter — a Rust obs.py analog to extend the contract to Plan 2.** Observability v1 is a JSON Schema contract, applicable to any language. A mini `arbiter-obs` crate is needed that emits OTel Logs Data Model JSONL + accepts `TRACEPARENT` (structure `arbiter-core/src/tracing.rs` or `arbiter-mcp/src/obs.rs`). The goal — so that the arbiter subprocess that Maestro spawns writes into the same `logs/<pipeline_id>/` and lands in `merged.jsonl`. **Anchor:** `arbiter/arbiter-core/`, `arbiter/arbiter-mcp/`, `Maestro/_cowork_output/observability-contract/propagation.md`.

6. **proctor-a — confirm status (stagnant or work-in-private).** 0 commits over 8 days after 91/8 — we need to understand whether the team is on pause or working in a private branch / another repository. Not a blocker, but useful for the ecosystem map. **Anchor:** `proctor-a/` — at the level of talking to the PM / project owner.

7. **agents-for-game — decide its fate (P2, carried over).** No `.git`, `.env` with real tokens, an obvious satellite of atp-platform. Options: (a) into atp-platform as `atp-playground/` (a bundled manual client), (b) a separate repository `atp-client-examples`, (c) full deletion + `.gitignore` at the root. The current state is a security risk. **Anchor:** `all_ai_orchestrators/agents-for-game/`.

8. **Maestro — release-drafter workflow for v0.3.0 prep (carried over from last week).** Still not done. Cheap win (single GHA job). **Anchor:** `Maestro/.github/workflows/`, new file `release-drafter.yml`.

---

**Data source:** `git log --since="2026-04-18"` across all repositories Maestro/arbiter/atp-platform/proctor-a/spec-runner/open-prose; `ls -la` + content inspection for `agents-for-game/` (not git); cross-checked with `_cowork_output/status/2026-04-18-status.md`, `Maestro/CHANGELOG.md`, `Maestro/_cowork_output/observability-contract/`, `Maestro/_cowork_output/status/obs-m1-reached-2026-04-19.md`, `arbiter/TODO.md`, `COWORK_CONTEXT.md`. TODO/FIXME counts — `grep -rnE "TODO|FIXME|HACK" --include="*.py|*.rs|*.ts|*.tsx|*.js|*.jsx"`, excluding `node_modules/target/.venv/.git/test_/tests/`.
