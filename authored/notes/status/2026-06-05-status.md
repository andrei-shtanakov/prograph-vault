---
title: Status Check — 2026-06-05
type: note
status: archived
owner: Andrei
updated: 2026-06-05
archived: 2026-07-06
reason: historical status snapshot (bulk-archived on graduation from _cowork_output)
---

# Status Check — 2026-06-05

> Weekly status check of the AI-orchestrators ecosystem.
> Window: 2026-05-29 → 2026-06-05. Comparison baseline: `_cowork_output/status/2026-05-29-status.md`.
> Method: `git log/status` across all repositories, grep TODO/FIXME, checking contract files.
> Mode: read-only, output only to `_cowork_output/status/`.

## TL;DR
1. **All of the week's activity is in spec-runner: 42 commits in a single day (2026-05-30), release v2.3.0.** The highlights: error classification (`error_kind`/`error_stage`), a codex-adapter fix (`codex exec` instead of `codex -p`), auto-disable of git automation for subdir projects, and a fix to `reset_failed_to_pending` (re-run of failed tasks). The other repositories — zero commits.
2. **The window's contract change: spec-runner v2.3.0 extended `executor-state.schema.json`** — added experimental fields `error_kind`/`error_stage` and new `executor_meta` keys (`7241e6c`). Additive; the Maestro consumer (`ExecutorState` with `extra="ignore"`, `Maestro/maestro/spec_runner.py:157`) does not break, the fixtures `maestro-interop/` were not touched. But the "frozen" R-04 contract is now evolving — Maestro does not read the new fields.
3. **R-07 (eval-driven routing) is still not started in code** — Maestro and arbiter have had no commits since 2026-05-23 (13 days). The "route cheapest sufficient agent from test results" loop has been idle for a second week after M4 unblocked it. R-06b M5 (CLI) is also pending.
4. **Registry drift not closed (P1 since 05-29):** `prograph`/`appgraph`/`prograph-vault` are still absent from COWORK_CONTEXT.md (updated 2026-05-23). appgraph has an uncommitted edit to `spec/tasks.md` (76/76 lines).
5. **proctor-a — 7+ weeks paused, open-prose — 8+ weeks.** The flag "move proctor-a to archived" has been hanging for a third report in a row.

---

## Project status (window 2026-05-29 → 06-05)

| Project | Commits in window | Last commit | Date | Uncommitted | State |
|--------|---:|------------------|------|---:|-----------|
| **spec-runner** | 42 | `6d16899` chore: sync version 2.3.0 in uv.lock | 2026-05-30 | 6¹ | 🟢 Release v2.3.0 + WIP Pi-templates |
| **Maestro** | 0 | `6574d32` docs(changelog): Copilot review PR #23 | 2026-05-23 | 2² | 🟡 13 days of silence after M4 |
| **arbiter** | 0 | `8270fc9` docs: Copilot review PR #16 | 2026-05-23 | 2 | 🟡 13 days of silence after M4 |
| **atp-platform** | 0 | `3a7234a` chore: prograph exclude | 2026-05-27 | 1 | 🟡 Silence |
| **prograph / appgraph** (root repo) | 0 | `4f77ad2` appgraph: fix tasks.md spec validation | 2026-05-28 | 38³ | 🟡 Silence; registry not updated |
| **proctor-a** | 0 | `53e8844` fix(tests): unused asyncio import | 2026-04-16 | 4 | 🔴 Paused 7+ wk |
| **open-prose** | 0 | `6284194` plagin test | 2026-04-07 | 3 | ⏸️ Idle 8+ wk |

> ¹ spec-runner WIP (not committed): new `docs/pi-workflow.md`, `templates/pi/`, edits to `spec-generator-skill` — looks like work is underway on the Pi Agent workflow.
> ² Maestro untracked: `docs/architecture/two-mode-architecture.{md,html}` — a new architecture document outside git.
> ³ In the root repo most `??` are nested git repositories and cowork files (normal); the substantive one: `M appgraph/spec/tasks.md` (76+/76−, uncommitted since 05-28).

### spec-runner v2.3.0 — what landed (all 42 commits dated 2026-05-30)
- **Error classification:** columns `error_kind` (rate_limit/auth/network/cli_error/unknown) and `error_stage` (sync_deps…review) in executor-state; displayed in `status` (`8000aa6`, `254b7c3`).
- **Fixes:** `0695e13` the codex adapter called `codex -p` (= `--profile`!) → now `codex exec`; `c37d451` `reset_failed_to_pending` resets attempts so that `run --all` actually re-runs failed tasks.
- **Subdir projects:** `002ca61`/`4fe7fce` — git automation auto-OFF if the project lives inside someone else's repo (a direct response to the appgraph/prograph situation in the root repo).
- **Housekeeping:** 5 dependabot merges, minimal GITHUB_TOKEN permissions, docs audit (`ae7c139`).

---

## Contract points

| Contract | Changes in window | Consumers |
|----------|------------------|-------------|
| **spec-runner `schemas/executor-state.schema.json`** | ⚠ **Additive bump** (`7241e6c`, v2.3.0): `error_kind`, `error_stage`, new `executor_meta` keys; marked experimental | 🟡 Maestro `ExecutorState` (`extra="ignore"`) — does not break, but does not consume the new fields; fixtures `tests/fixtures/maestro-interop/` were NOT updated |
| arbiter MCP (6 tools, protocol 1.1.0) | no changes | ✅ pin held |
| `arbiter/config/agents.toml`, `arbiter-core/src/types.rs`, `obs.rs` | no changes | ✅ |
| `atp-platform/.../protocol/models.py` | no changes | ✅ |
| Observability `log-schema.json` / `obs.py @ fa6b106` | no changes | ✅ 4-way axis aligned |

**Contract conclusion:** the only event is the additive evolution of executor-state in spec-runner. R-04 is formally "frozen", but de facto the contract is growing: worth deciding whether to (a) update the interop fixtures and document the v2.3.0 fields as stable/experimental on the Maestro side, or (b) explicitly record that Maestro ignores the experimental fields until stabilization. The `error_kind`/`error_stage` fields are a potentially valuable input for arbiter routing (classified agent-failure reasons are a direct signal for R-07).

### TODO/FIXME
Genuine code debt unchanged — the same 3 items: `arbiter/orchestrator/arbiter_client.py:78`, `atp-platform/atp/cli/commands/push.py:122`, `Maestro/tests/test_git_worktree.py:316`.

---

## Brief status

- **What progressed:** spec-runner v2.3.0 — classification of execution errors, a codex-adapter fix, correct re-run of failed tasks; uncommitted work on the Pi Agent workflow is underway. It was the only project that moved this week.
- **What is blocked / idle:** R-07 (eval-driven routing) was unblocked back on 05-23, but Maestro/arbiter have been standing still for 13 days — the benchmark→routing loop is not closed. R-06b M5 (CLI `maestro benchmark`) is pending. Maestro M3 observability is pending.
- **What needs attention:** (1) the COWORK_CONTEXT registry is not updated — the P1 from last week is not closed; (2) R-04 drift: the executor-state schema grew, the fixtures are not synchronized; (3) Maestro `two-mode-architecture.md` sits outside git — at risk of being lost; (4) proctor-a → archived (third week of the flag); (5) the uncommitted `appgraph/spec/tasks.md` has been hanging since 05-28.

---

## Recommended actions

| Priority | Project | Action | Rationale |
|-----------|--------|----------|-------------|
| **P1** | COWORK_CONTEXT.md | Add prograph/appgraph/prograph-vault to the registry; update spec-runner to v2.3.0 (the registry lists 2.0.0) | Carried over from the 05-29 report; the registry is already 2 versions of spec-runner behind |
| **P1** | Maestro ↔ spec-runner | Decide the fate of R-04 after v2.3.0: synchronize the `maestro-interop` fixtures + document `error_kind`/`error_stage` as experimental on the consumer side | The "frozen" contract is evolving one-sidedly (`7241e6c`) |
| **P1** | Maestro/arbiter (R-07) | Start eval-driven routing; consider `error_kind` from spec-runner as an additional signal to `BenchmarkResult` | 13 days of idle on the unblocked loop; classified failures are a ready feature for the DT |
| **P2** | Maestro | Commit `docs/architecture/two-mode-architecture.{md,html}` | Architecture document outside VCS |
| **P2** | proctor-a | Move to `📦 archived` in the registry | 7+ wk paused; flag since 05-22 |
| **P3** | appgraph | Commit or revert `spec/tasks.md` (76/76 lines, hanging since 05-28) | Uncommitted spec change in the root repo |

---

*Sources: `git log/status` across Maestro, arbiter, atp-platform, spec-runner, proctor-a, open-prose, the root repo (prograph/appgraph); `spec-runner@7241e6c` (schema diff); `Maestro/maestro/spec_runner.py:157`; `COWORK_CONTEXT.md`; `_cowork_output/status/2026-05-29-status.md`.*
