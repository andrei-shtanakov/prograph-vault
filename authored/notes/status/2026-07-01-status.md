---
title: Status — 2026-07-01
type: note
status: archived
owner: Andrei
updated: 2026-07-01
archived: 2026-07-06
reason: historical status snapshot (bulk-archived on graduation from _cowork_output)
---

# Status — 2026-07-01

> Method: `git log/status` + reading code across Maestro, arbiter, atp-platform (read-only).
> Previous status: `status/2026-06-26-status.md`. The one-day delta is large.

## TL;DR

1. **A day of implementation: 12 PRs across three repos.** Both axes designed in the specs from 2026-07-01 were closed — **ADR-ECO-003 (agents catalog)** and **R-07 track B slice A (rank_score tiebreaker)** — plus **D1/D2 in Maestro**.
2. **The agents catalog landed in ATP + arbiter.** Three copies of `agents-catalog.toml` (SSOT + 2 vendored) are **byte-for-byte identical**; there is a conformance check (`_cowork_output/devtools/check-agent-id-conformance.py`).
3. **The R-07 chain is built, but inert:** ATP emits `rank_score`, arbiter reads it deterministically — however `ARBITER_BENCH_WEIGHT` is still `0.0` → re-rank in prod is a **no-op**. For the first time there is separating data (breakpoint), but the switch is not flipped.
4. **Two coherence holes:** (a) Maestro is **not tied** to the catalog (`DEFAULT_*_MODEL` hardcoded); (b) the conformance check covers only ATP+arbiter (pre-D2 variant).
5. **The critical path for the next session is short:** raise the R-07 weight and run A/B — it depends on nothing and produces a result immediately.

## Merged today

| Repo | PR | What |
|---|---|---|
| **Maestro** | `#35` (745f012) | D2 opened the `AgentType`→registry gate + D1 routed-model passthrough in `spawn()` |
| Maestro | `#37` (894b4c9) | fix: `AWAITING_APPROVAL → READY` (not RUNNING) |
| Maestro | `#36` (d9b3efc) | docs audit/sync |
| Maestro | `#34` (8f3c07d) | deps: pydantic-settings 2.12→2.14.2 |
| **arbiter** | `#30` (eec1879) | R-07: consumption of `rank_score` + **deterministic** re-rank |
| arbiter | `#29` (c8e190e) | ADR-ECO-003: generator-scaffold `agents.toml` from the catalog |
| arbiter | `#31` (9eb1f3f) | docs audit/sync |
| arbiter | `#28` (5a8d75f) | deps: pydantic-settings →2.14.2 |
| **atp-platform** | `#210` (c912532) | R-07: emission of `rank_score` + `bp_ordinal` tiebreaker |
| atp-platform | `#211` (8aba0e5) | ADR-ECO-003: catalog-driven roster (ATP slice, AI#2) |
| atp-platform | `#212` (78d92f7) | docs audit/sync |
| atp-platform | `#209` (10d12bf) | CI: actions/checkout 4→7 |

## State of the two axes

### ADR-ECO-003 — agents catalog

| Item | Status | Reference |
|---|---|---|
| SSOT catalog | ✅ | `_cowork_output/contracts/agents-catalog.toml` |
| ATP generates roster from the catalog (AI#2) | ✅ | `atp-platform/method/agents-catalog.toml` (== SSOT) |
| arbiter scaffolds `agents.toml` from the catalog | ✅ | `arbiter/scripts/gen_agents_scaffold.py`, `arbiter/config/agents-catalog.toml` (== SSOT) |
| conformance check | 🟡 pre-D2 variant | `_cowork_output/devtools/check-agent-id-conformance.py` (ATP+arbiter, without Maestro) |
| **Maestro generates `DEFAULT_*_MODEL` from the catalog (AI#4)** | ❌ pending | hardcode `Maestro/maestro/spawners/claude_code.py:21`, `codex.py:21` |

### R-07 track B slice A — rank_score tiebreaker

| Item | Status | Reference |
|---|---|---|
| ATP emits `rank_score` in `score_components` | ✅ | `atp/reporters/benchmark_reporter.py` |
| arbiter reads `rank_score` (fallback to `score`) | ✅ | `arbiter/arbiter-mcp/src/db.rs` `get_benchmark_score` |
| determinism of the residual tie by `agent_id` | ✅ | `route_task.rs:82` + tests `rerank_breaks_residual_ties_by_agent_id`, `rerank_orders_tied_scalar_agents_by_rank_score` |
| **enabling in prod (`weight > 0`) + A/B** | ❌ pending | `ARBITER_BENCH_WEIGHT` default `0.0`, `route_task.rs:349` |

## Queue for the next session (by project)

### arbiter
- **[P0] Enable R-07 A/B** — raise `ARBITER_BENCH_WEIGHT > 0` (`route_task.rs:349`), run A/B. Depends on nothing, the most valuable step.
- **[P1] Extend conformance to Maestro** — after tying Maestro to the catalog (depends on Maestro P1).

### Maestro
- **[P1] Tie to the catalog (ADR-003 AI#4)** — generate `DEFAULT_*_MODEL` from `agents-catalog.toml` instead of hardcode (`spawners/claude_code.py:21`, `codex.py:21`). D2 merged (`#35`), so this is unblocked. Precedes arbiter P1.
- **[P1] Pick up the D1 remark that didn't land** — warn/validation on an unknown routed model (not in `#35`; a bad model goes straight into `--model`). Independent.

### atp-platform
- **[P2] Fix token accounting of the `claude_code` shim** (their TODO) — on the 06-21 run tokens=1336 is implausibly low; cost is correct.

### Cross-cutting / _cowork_output
- **[P2] COWORK_CONTEXT** — reflect ADR-003 (catalog) and R-07 slice A in the registry/roadmap.
- **[P3] Housekeeping (edits in the repos — for a human):** add `.claude/settings.local.json` to `.gitignore` (all three), arbiter `logs/` and `.fuse_hidden*`. Pure `git status` hygiene.

## Order with dependencies

1. **arbiter P0** (flip weight + A/B) — independent, do it first.
2. **Maestro P1 (catalog)** → **arbiter P1 (conformance for Maestro)** — strictly in this order.
3. **Maestro P1 (D1 warn)** and **ATP P2 (tokens)** — in parallel, independent.
4. Registry + housekeeping — in between.

## Recommended actions

- **arbiter:** first remove the R-07 inertness — raise `ARBITER_BENCH_WEIGHT` and measure A/B on separating data.
- **Maestro:** close the catalog coherence hole (AI#4) — otherwise SSOT covers 2 of 3 consumers; then extend conformance.
- **Maestro:** add a warn on an unknown routed model (debt from the D1 review).
- **atp-platform:** minor fix of the shim's token accounting.
- **_cowork_output:** update the registry; housekeeping `.gitignore` — on the human side (repos are read-only).
