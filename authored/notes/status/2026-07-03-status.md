---
title: Status — 2026-07-03
type: note
status: archived
owner: Andrei
updated: 2026-07-03
archived: 2026-07-06
reason: historical status snapshot (bulk-archived on graduation from _cowork_output)
---

# Status — 2026-07-03

> Method: `git log/status` + byte comparison of contract files (read-only).
> Previous status: `status/2026-07-01-status.md`. Trigger: scheduled `weekly-check-projects-status`.
> The two-day delta is large (28+ commits across 4 repos).

## TL;DR

1. **Both "pending" holes from the previous report are closed.** Maestro is tied to the catalog (AI#4, `#38`), ATP fixed the shim's token accounting (`#213`). Plus the SSOT canon was formalized: `atp-platform/method/agents-catalog.toml` — the single source (ADR-ECO-003, canon since 2026-07-03).
2. **⚠️ A new coherence gap (top-attention):** `arbiter/config/agents-catalog.toml` **got AHEAD of the SSOT**. arbiter commit `#37` (21 min ago) added to the vendored copy `mimo-v2.5-free`, `opencode routable=true` and Path B pairs that are **not in the ATP SSOT**. Conformance Check 2 (byte-for-byte) **fails** at byte 2340 / line 50.
3. **A new OPEN harness opencode** landed in three repos (Maestro spawner `979f943`, ATP shim+catalog, arbiter policy). But its `routable` status is **out of sync**: ATP says `false` (pilot, flip only after a crossover with runs≥3), arbiter — `true`. This is the essence of the drift from item 2.
4. **R-07 re-conceived, but still inert in prod.** Re-rank was re-scoped as **tiebreaker-only** (decision C, arbiter `#34`), there is a real test flip code-review→codex (`#33`). But `ARBITER_BENCH_WEIGHT` default is still `0.0` (`route_task.rs:362`) → a no-op in prod.
5. **spec-runner released 2.8.0** (VSCode read-surface contracts `#30`, gated spec generation `#28`). TODO/FIXME debt grew to 18 — worth triaging.

## Merged since the previous report (2026-07-01 → 2026-07-03)

| Repo | PR | What | Significance |
|---|---|---|---|
| **Maestro** | `#38` (048a530) | catalog-driven model defaults (**AI#4**, ADR-ECO-003b) | ✅ closed the P1 coherence hole |
| Maestro | `979f943` | opencode spawner — first routable OPEN harness | 🆕 new harness |
| Maestro | `#40` (d67a399) | docs: `_cowork_output` dev-only; defaults from the ATP-hosted catalog | canon |
| Maestro | `#39` (7a48173) | docs audit/sync | — |
| **arbiter** | `#37` (4f242a0) | vendor SSOT sync — opencode routable + Path B pairs | ⚠️ **source of drift** |
| arbiter | `#34` (1fbbdf7) | R-07 decision C: re-rank as **tiebreaker** | scope-lock |
| arbiter | `#33` (3c307ad) | test: rerank flips code-review→codex on real re-sweep scores | ✅ real data |
| arbiter | `#32` (6ee2f39) | retire dead agent keys (ADR-ECO-003a) | housekeeping |
| arbiter | `#36` (a9bdf93) | docs: config copy vendors the ATP SSOT | canon |
| **atp-platform** | `#213` (c5132fe) | fix: cache tokens in `total_tokens` of the claude_code shim | ✅ closed P2 |
| atp-platform | `#215` (f8c99f5) | golden-suite lock on the code-review case set (003a D3) | ✅ stabilization |
| atp-platform | `#216` (bdbe6cd) | req-extraction ladder for benchmark#2 crossover | R-07 data |
| atp-platform | `#214` (2b7ce32) | dead routable keys → `status=retired` (003a) | housekeeping |
| atp-platform | `#217` (56dc020) | fix: skip `read_only_corpus` in the CLI-adapter pipe-check | — |
| atp-platform | `#219/#220` | docs: SSOT canon; rename `_cowork_output`→`_bench_output` | canon/cleanup |
| **spec-runner** | `#30` (0566b1b) | VSCode read-surface contracts + release **2.8.0** | 🆕 release |
| spec-runner | `#28` (592528f) | gated spec generation (opt-in governance) | 🆕 feature |

## Contract points

### agents-catalog.toml (SSOT, ADR-ECO-003) — ⚠️ DRIFT

| Copy | Role | Status vs SSOT |
|---|---|---|
| `atp-platform/method/agents-catalog.toml` | **SSOT (owner)** | — canon |
| `arbiter/config/agents-catalog.toml` | vendored (should be byte-for-byte) | ❌ **DRIFT** (ahead: +`mimo-v2.5-free`, `opencode routable=true`, Path B qwen/mimo) |
| `Maestro/tests/fixtures/agents-catalog.toml` | test fixture (Maestro pulls ATP-hosted, `#40`) | ⚠️ third md5; outside Check 2 — confirm this is not a contract copy |

- **Symptom:** `cmp atp-platform/method/agents-catalog.toml arbiter/config/agents-catalog.toml` → differ at byte 2340, line 50.
- **Cause:** arbiter `#37` (2026-07-03, 21 min) introduced opencode-routable + Path B into the vendored copy **earlier** than it landed in the SSOT owner (ATP). The CLAUDE.md rule was violated: "cross-repo contracts are vendored inward as a pinned copy — the SSOT lives in the owner repo".
- **A detector exists, but:** `_cowork_output/devtools/check-agent-id-conformance.py` Check 2 catches this (byte-for-byte, lines 194–210) and already covers Maestro (16 mentions). It requires Python ≥3.11 (`tomllib`) — on the 3.10 environment it fails on import before the check runs. **CI must run on 3.11+**, otherwise the gate silently no-ops.

### opencode routable — status out of sync

| Repo | File | routable | Condition |
|---|---|---|---|
| atp-platform | `method/agents-catalog.toml:245` | `false` | flip to true ONLY after a crossover with runs≥3 (gate 003a D4) |
| arbiter | `config/agents-catalog.toml` | `true` | "pilot ADR-003c: agentic OPEN harness; the Maestro spawner exists" |

The two repos **contradict each other** on whether opencode is already routed right now. An owner decision (ATP) is needed: either the ATP SSOT raises opencode to routable (if the crossover gate has passed), or arbiter rolls the vendored copy back to `false`.

### R-07 benchmark re-rank — inert in prod

- `ARBITER_BENCH_WEIGHT` default `0.0` → `apply_benchmark_rerank` is a no-op (`arbiter/arbiter-mcp/src/tools/route_task.rs:362`).
- The semantics **narrowed**: decision C (arbiter `#34`) — re-rank is now only a tiebreaker, not full weighting. The P0 from the previous report ("raise weight + A/B") needs reformulating under the tiebreaker mode.

## Brief status

- **Progressed:** both pending holes were closed (Maestro catalog AI#4, ATP tokens); the SSOT canon was formalized across all 4 repos; the opencode harness landed; R-07 was re-scoped as a tiebreaker with a real test flip; spec-runner 2.8.0.
- **Blocked / inert:** R-07 A/B in prod (weight=0.0); the opencode→routable flip awaits the crossover gate runs≥3.
- **Needs attention:** (1) **drift of the arbiter catalog from the SSOT** — Check 2 is red; (2) the opencode `routable` contradiction between ATP and arbiter; (3) the third md5 of the Maestro fixture — confirm it is outside the contract; (4) conformance CI must run on Python ≥3.11.

## Recommended actions

- **atp-platform (owner, P0):** decide the fate of the drift — if opencode-routable + Path B pairs are legitimate, add them to `method/agents-catalog.toml` (SSOT) so the arbiter copy becomes byte-for-byte again. Otherwise — signal arbiter to roll back.
- **arbiter (P0):** until the SSOT is synced, roll `config/agents-catalog.toml` `#37`-delta back to the SSOT state (`opencode routable=false`, without `mimo-v2.5-free`), or wait for the ATP update. Do not leave Check 2 red.
- **_cowork_output/devtools (P1):** lock the Python ≥3.11 requirement into the conformance gate CI (right now on 3.10 it fails on `import tomllib` before Check 2 → false green).
- **Maestro (P2):** confirm that `tests/fixtures/agents-catalog.toml` is intentionally test-only (Maestro pulls ATP-hosted, `#40`) and should not participate in the byte-for-byte contract.
- **arbiter (P2):** remove the R-07 inertness already in the tiebreaker semantics — run A/B on separating data (`#33` shows the flip is real).
- **spec-runner (P3):** triage TODO/FIXME (18, up from the previous report).
- **COWORK_CONTEXT.md (P3):** reflect the SSOT canon (method/agents-catalog.toml), the opencode harness and ADR-ECO-003a/b/c — the registry lags (last updated 2026-05-23).

---

### Housekeeping (repos read-only — for a human)

`.claude/settings.local.json` is not in `.gitignore` in any repo; in arbiter, `arbiter.db.bak-*`, `logs/`, `.fuse_hidden*` are dangling in the working tree. Pure `git status` hygiene.

### TODO/FIXME (source, excluding tests/_cowork_output)

| Repo | Count | Δ |
|---|---|---|
| Maestro | 3 | ~ |
| arbiter | 4 | ~ |
| atp-platform | 2 | ↓ |
| spec-runner | 18 | ↑ (needs triage) |
