.DS_Store

# Obsidian Local REST API — contains apiKey + TLS private key, never commit
.obsidian/plugins/obsidian-local-rest-api/data.json

# Obsidian per-session UI state — changes on every open/click/zoom, never commit
.obsidian/workspace.json
.obsidian/graph.json
